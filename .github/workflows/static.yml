# Deprecated — superseded by main.yml (Astro + GitHub Pages).
# Kept as a disabled, manual-only no-op. Safe to delete in the repo UI.
name: Deprecated static deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. Deployment is handled by main.yml."
