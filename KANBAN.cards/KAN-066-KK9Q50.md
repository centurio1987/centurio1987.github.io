---
card: KAN-066-KK9Q50
title: 폭신 대담: 대화 원본 → 정형 구조화 저장 기능 (원문 보존 포함)
created: 2026-08-16
scope: raws/talks/**, draft/ddd-transaction-account-discussion-draft.mdx, src/lib/talk.ts, src/lib/talkSource.ts, scripts/build-talk.ts, scripts/verify-talk.ts, .claude/skills/talk-ingest/**, src/templates/talk-episode.mdx, .claude/skills/init-post/SKILL.md, .claude/authoring/paths.json, package.json
---

# KAN-066-KK9Q50 — 폭신 대담: 대화 원본 → 정형 구조화 저장 기능 (원문 보존 포함)

## 전략
<!-- 왜 이 접근인가 · 제약 · 버린 대안. 사람이 자유롭게 편집한다. -->

**입력의 실물을 먼저 봤다.** `raws/ddd-transaction-account-discussion.mdx` 가 유일한
대화 원본 표본이고 93KB 다. 그 안은 중첩 JSON 그래프(`{id, step_index, source, type,
content, tool_calls, children}`)이며 `source` 는 `USER_EXPLICIT` · `MODEL` · `SYSTEM`,
`type` 은 `USER_INPUT` · `CONVERSATION_HISTORY` · `EPHEMERAL_MESSAGE` 등으로 갈린다.
사람의 발화보다 시스템 프롬프트 · 도구 호출 · 플래너 잡음이 훨씬 많다. 이 상태로는
`<QA n q>` 골격에 못 넣고, 매번 집필자 컨텍스트에 93KB 를 통째로 올려야 한다.

**목적지도 이미 정해져 있다.** `.claude/authoring/specs/polssin-daedam/spec.json` 의
`sections` 가 `frontmatter` · `qa`(repeat) · `pullquote` 셋이고, 원칙 SP1(`n` 은 1부터
연속) · SP4(PullQuote 는 요약이 아닌 도달점, 1~2개) · SP5(`template: talk` · `series` ·
`order`) · SP6(`author` 는 인터뷰어)은 **전부 기계가 검사할 수 있는 계약**이다.
즉 발행물은 사람이 손으로 쓸 것이 아니라 **구워야 할 것**이다.

### 3계층으로 나눈다

카드가 요구한 "원문/교정본 2계층"에 **발행물 1계층을 더해 3계층**으로 간다.
2계층으로 끝내면 교정본이 곧 발행물이 되어, 발행물에 손을 대는 순간 "다시 굽기"가
깨지기 때문이다.

| 층 | 무엇 | 어디 | 누가 고치나 |
|---|---|---|---|
| L1 원문 | 받은 그대로. 불변 | `raws/talks/<slug>/source.<ext>` + `source.json` | 아무도 |
| L2 정형본 | 노이즈 제거 + 교정된 정본 | `raws/talks/<slug>/talk.md` | 사람 · AI |
| L3 발행물 | 파생. 손대지 않는다 | `src/content/posts/talk-<slug>.mdx` | 빌더만 |

L1→L2 는 판단이 드는 일(무엇이 질문이고 무엇이 잡음인가, 톤앤매너·비문·맞춤법)이라
**스킬**이 맡는다. L2→L3 는 계약 검사와 치환뿐이라 **스크립트**가 맡는다.
"스킬로 만들지 스크립트로 만들지"는 양자택일이 아니라 이 경계선의 문제였다.

### 굳힌 판단 넷

1. **L2 는 markdown 이다 (JSON·YAML 아님).** 대담 정본은 사람이 계속 손볼 물건인데
   긴 한국어 답변을 JSON 이스케이프나 YAML 블록 스칼라에 가두면 편집이 무너진다.
   대신 형식이 흐물해지지 않도록 **Zod 파서**로 계약을 잡는다 — `src/lib/viz/schema.ts`
   가 ```viz``` 블록에 쓴 것과 같은 방식이다.
2. **저장 위치는 `raws/talks/<slug>/` 다.** `raws/README.md` 가 raws 를 "사용자가 직접
   작성하는 공간"으로 정의하고 `raws/_proceeded/` 로 봉인하는 관례가 이미 있다.
   별도 루트 디렉터리를 새로 파면 유저 입력의 입구가 둘이 된다.
3. **`init-post --talk` 와 합치지 않는다.** 그쪽은 원본 없이 바로 쓰는 **빈 스켈레톤**
   경로이고 이쪽은 **내용이 찬 것을 굽는** 경로다. 진입점이 다르므로 둘 다 남기고,
   `src/templates/talk-episode.mdx` 를 두 경로의 단일 소스로 공유한다.
4. **L3 는 사람이 안 고친다.** 고치고 싶으면 L2 를 고쳐 다시 굽는다. 이 규약이 없으면
   카드가 요구한 "개선 방향이 바뀔 때 다시 굽는다"가 첫 수정에서 죽는다.

### 버린 대안 셋

- **원본을 raws 에 그대로 두고 집필자가 읽어 쓴다**(현행) — 93KB 노이즈를 매번 올려야
  하고, 같은 원본에서 같은 결과가 두 번 나오지 않는다.
- **원본 → MDX 직행(2계층)** — 교정본이 곧 발행물이라 재생성이 불가능하다. 카드
  요구사항 ③ 을 정면으로 어긴다.
- **L2 를 JSON/YAML 로** — 기계 친화적이지만 사람이 못 고친다. 정본은 사람이 고치는
  물건이라는 전제와 충돌한다.

### 열린 위험 둘

- **표본이 1건이다.** 다른 export 형식(ChatGPT · Claude · 평문 붙여넣기)이 오면 S1 의
  정규화 규칙이 흔들린다. 그래서 파서를 형식별 어댑터로 나누고, 모르는 형식은
  **추측하지 않고 실패시킨다**.
- **KAN-067 과 맞물린다.** 그 카드의 대담 준비 노트가 어디에 앉을지가 정해지면
  `raws/talks/<slug>/` 안에 `prep.md` 로 들어올 수 있다. 지금은 자리만 비워 둔다.

## 실행 계획
<!-- `S<n>`은 고정 id — 이름을 바꾸지 않는다. 체크 상태는 doc-step 이 갱신한다. -->

- [x] `S1` 원본 정규화 규칙 확정 — `raws/ddd-transaction-account-discussion.mdx` 를 표본으로 노이즈 계층(`SYSTEM`·`EPHEMERAL_MESSAGE`·`tool_calls`·`USER_SETTINGS_CHANGE`)을 전수 분류하고 "무엇을 버리고 무엇을 남기는가"를 규칙 문서로 고정. **완료 기준**: 규칙 문서 + 표본에서 살아남은 발화 수·바이트 수 실측값이 문서에 박힌다
- [x] `S2` L1 원문 보존 규격 — `raws/talks/<slug>/` 레이아웃과 `source.json`(출처·모델·수집일·형식·sha256) 스키마 확정. **완료 기준**: 표본 1건을 실제로 그 구조로 앉히고, 해시 재계산이 기록값과 일치
- [x] `S3` L2 정형 스키마 + 파서 — `talk.md` 문법(frontmatter + Q/A 마커) 정의와 `src/lib/talk/talkSource.ts`(Zod). **완료 기준**: 표본 파싱 성공 + 위반 케이스 5종(n 불연속·q 누락·PullQuote 3개·series 누락·author 가 교수님)이 각각 **서로 다른** 에러 메시지로 실패
- [x] `S4` L2 → L3 빌더 — `scripts/build-talk.ts` 가 `talk.md` 를 `src/content/posts/talk-<slug>.mdx` 로 굽는다. SP1·SP4·SP5·SP6 계약 검사 내장, 템플릿은 `src/templates/talk-episode.mdx` 공유. **완료 기준**: 표본을 구워 `bun run build` 통과 + `check-post-markers`·`check-emphasis --dist` 통과
- [x] `S5` 검증기 — `scripts/verify-talk.ts` + `package.json` 의 `talk:verify`. L2↔L3 어긋남(손으로 고친 발행물)과 L1 해시 불일치를 잡는다. **완료 기준**: 일부러 깨뜨린 3케이스가 전부 비0 종료, 정상 케이스는 0
- [x] `S6` `talk-ingest` 스킬 — 원본 투입 → L1 봉인 → L2 초안(노이즈 제거·질문 재구성) → `authoring-kit` 의 `polssin-daedam` spec + `ppangtolab-prof` voice 로 톤앤매너·비문·맞춤법 교정 → `build-talk` 호출. **완료 기준**: 표본 1건이 원본부터 발행 가능한 MDX 까지 한 흐름에 통과하고, L1 이 한 바이트도 안 바뀐다
- [x] `S7` 접점 문서화 — `CLAUDE.md` 폭신 대담 절, `init-post` 스킬의 경계 한 줄, `.claude/authoring/paths.json` 의 `dirs.talk`. **완료 기준**: 세 곳 갱신 + `bun run build` 재통과

## 검증
<!-- 무엇을 실행해 무엇이 나오면 이 카드가 끝난 것인가. -->

아래가 전부 0으로 끝나면 끝난 것으로 본다. 하나라도 비0이면 미완이다.

```bash
bun run talk:verify        # 3계층 어긋남 + 픽스처 회귀 + 자가검사 14종
bun scripts/build-talk.ts --all --check   # 발행물이 정형본에서 구운 것과 같은가
bun run build              # 전 페이지 빌드
bun scripts/check-post-markers.ts
bun scripts/check-emphasis.ts && bun scripts/check-emphasis.ts --dist
bunx tsc --noEmit          # 워크트리에서는 bunx astro sync 를 먼저
```

**2026-08-16 실행 결과 (전부 통과).** `talk:verify` 0 · `build-talk --all --check` 0
(건너뜀 1 — `ddd-entry-contradiction` 은 정형본 없음) · `build` 46쪽 · 마커 30 ·
강조 소스 30/렌더 46 · `tsc` 0.

**눈으로 확인해야 하는 것 셋** — 스크립트가 판정 못 한다. 셋 다 확인 완료.

1. **대담 레이아웃이 실제로 뜨는가.** 픽스처를 `src/content/posts/` 로 옮기고
   `draft: false` 로 `bun run build` → `/posts/talk-minimal/` 생성, `Q.01`·`Q.02` 자동
   번호, `<section class="qa">` 2개, `pull-quote` 1개, 화자 듀오(빵토 교수님), `마치며`
   확인. 확인 후 그 글은 지웠다 — 가짜 대담을 사이트에 남기지 않는다.
2. **정형본 한 문장을 고치면 그 문장만 바뀌는가.** 픽스처 `talk.md` 의 한 문장을 바꿔
   다시 구운 결과를 diff → 그 한 줄만 달랐다.
3. **원문이 파이프라인 전 구간에서 안 움직이는가.** 픽스처 원본을 `ingest-talk` 로
   봉인하고 굽기까지 완주한 뒤 sha256 재계산 → 투입 원본·봉인본 모두 동일.

**남은 것 하나 — 실물 원본으로 도는 완주.** 유일한 실물 원본
`raws/talks/ddd-entry-contradiction` 은 답변 5개가 잘려 있어(유실 7,527 B = 26.8%)
파이프라인이 정당하게 거부한다. 원본을 다시 받으면 그때 실제 에피소드 1편으로 확인한다.
이 카드의 산출물 자체는 그것과 무관하게 완성돼 있다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-08-16T01:17 · s:b780ef10 — `전략` 섹션 교체
- 2026-08-16T01:17 · s:b780ef10 — `실행 계획` 섹션 교체
- 2026-08-16T01:17 · s:b780ef10 — `검증` 섹션 교체
- 2026-08-16T01:52 · s:b780ef10 · S1 doing — 착수
- 2026-08-16T01:55 · s:b780ef10 · S1 done — 정규화 규칙 확정 — raws/talks/_rules/NORMALIZE_RULES.md. 표본 51노드 중 남는 발화 11개(23,451B, 23.4%). 답변 5개 전부 <truncated N bytes> 로 잘려 있어(유실 7,527B = 26.8%) 이 표본은 정본 불가 — 하드 실패 조건으로 박음. 민감 패턴은 버리기 규칙이 29건 전부 걷어냄(남길 노드 0건).
- 2026-08-16T01:55 · s:b780ef10 — 표본 raws/ddd-transaction-account-discussion.mdx 는 답변이 중간부터 잘려 있어 대담 정본으로 못 쓴다 — 원본을 다시 받아야 한다.
- 2026-08-16T01:56 · s:b780ef10 · S2 doing — 착수
- 2026-08-16T01:58 · s:b780ef10 · S2 done — L1 규격 확정 — raws/talks/README.md(레이아웃·source.json 스키마) + 표본을 raws/talks/ddd-entry-contradiction/ 으로 이전. sha256 재계산 일치(100,125B). integrity.usable=false 로 잘림 표시. 이전에 딸려 깨진 draft/ddd-transaction-account-discussion-draft.mdx 의 참조 2곳도 갱신.
- 2026-08-16T02:00 · s:b780ef10 · S3 doing — 착수
- 2026-08-16T02:04 · s:b780ef10 · S3 done — L2 문법·파서 확정 — raws/talks/_rules/TALK_MD.md(정본) + src/lib/talkSource.ts(Zod). 위반 케이스 9종이 전부 서로 다른 에러코드·메시지로 실패(요구 5종 초과), 정상 케이스 파싱 OK, tsc 0. 계획의 'n 불연속'은 문법에서 번호를 없애 애초에 못 일어나게 했고(검출보다 나음) 그 자리를 NO_QA·TEMPLATE_IN_FRONTMATTER 등으로 채웠다. 파서 위치는 src/lib/talk/ 가 아니라 src/lib/talkSource.ts — 기존 talk.ts 와 디렉터리/파일 이름이 충돌한다.
- 2026-08-16T02:04 · s:b780ef10 — 워크트리에서 tsc 를 돌리려면 bunx astro sync 를 먼저 해야 한다 — astro:content 가상 모듈 타입이 체크아웃마다 새로 생성된다.
- 2026-08-16T12:14 · s:b780ef10 · S4 doing — 착수
- 2026-08-16T12:20 · s:b780ef10 · S4 done — 빌더 scripts/build-talk.ts 완성 — L1 해시·integrity 검사 + talk.md 파싱 + 템플릿 모양으로 굽기(template: talk 은 여기서만 박음). 픽스처를 raws/talks/ 밖(scripts/fixtures/talk/)에 두려고 --talks-dir/--out-dir 를 받게 했다. 검증: 픽스처를 src/content/posts/ 로 옮겨 draft:false 로 bun run build → /posts/talk-minimal/ 렌더 확인(Q.01·Q.02 자동번호·pull-quote 1·화자듀오·마치며), check-post-markers 31 · check-emphasis 소스 31/렌더 46 · tsc 0. 확인 후 그 글은 삭제.
- 2026-08-16T12:22 · s:b780ef10 · S5 doing — 착수
- 2026-08-16T12:22 · s:b780ef10 · S5 done — 검증기 scripts/verify-talk.ts + package.json 의 talk:build·talk:verify. 저장소 대담·픽스처 회귀·자가검사(파서 위반 9종 코드별 판정 + 고장 주입 5종) + 고아 발행물 검출. 음성 검사: expected.mdx 훼손·고아 발행물 각각 exit 1, 정상 exit 0. tsc 0.
- 2026-08-16T12:26 · s:b780ef10 · S6 doing — 착수
- 2026-08-16T12:30 · s:b780ef10 · S6 done — talk-ingest 스킬 + scripts/ingest-talk.ts. 정규화를 스킬 산문이 아니라 스크립트로 냈다(결정론이라). 어댑터 antigravity-graph-mdx 1종, 모르는 형식은 추측 없이 실패. 민감 패턴은 복사 전에 막아 봉인 자체를 안 한다(잘림과 달리 유출은 되돌릴 값이 없다 — NORMALIZE_RULES §4 갱신). 완주 검증: 픽스처 원본 투입 → 봉인 → 굽기 → 재굽기 동일 → L1 sha256 불변 확인. 실물 원본으로 도는 완주는 원본 재수집 후. NORMALIZE_RULES 를 스킬 자산으로 옮기지 않았다 — TALK_MD 와 갈라지면 글쓴이가 두 군데를 뒤진다. 유저레벨 동명 스킬 없음.
- 2026-08-16T12:31 · s:b780ef10 · S7 doing — 착수
- 2026-08-16T12:33 · s:b780ef10 — `검증` 섹션 교체
- 2026-08-16T12:33 · s:b780ef10 · S7 done — 접점 3곳 반영 — CLAUDE.md 폭신 대담 절에 3계층·굳힌 판단 6개, init-post SKILL.md 에 talk-ingest 와의 경계(원본 있으면 그쪽), .claude/authoring/paths.json 에 dirs.talk. 겸사겸사 build-talk --all 이 정형본 없는 대담에 실패하던 것을 건너뛰기로 고쳤다(원문만 봉인한 상태는 정상이지 실패가 아니다). 전체 게이트 통과.
