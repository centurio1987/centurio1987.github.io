---
card: KAN-072-CPJCT1
title: UI 토큰 정합화 — 게이트가 무는 1,786건을 구획 단위로 0 으로 줄인다
created: 2026-08-25
scope: CLAUDE.md, scripts/tokens-baseline.json, scripts/verify-tokens.ts, scripts/lib/tokens/**, scripts/fixtures/tokens/**, src/components/**, src/pages/**, src/layouts/**, src/styles/tokens.css, src/styles/global.css, src/styles/deco.css, src/styles/viz-frame.css, src/styles/viz.css, src/lib/categories.ts, src/lib/doodleMarks.ts, src/lib/viz/Comparison.tsx, src/lib/viz/PosterHero.tsx, src/lib/viz/ProcessSteps.tsx, src/lib/viz/layout.ts, design-concept/DESIGN_CONCEPT.md, design-concept/DECO_KIT.md, design-concept/DIAGRAM_STYLE_GUIDE.md
---

# KAN-072-CPJCT1 — UI 토큰 정합화 — 게이트가 무는 1,786건을 구획 단위로 0 으로 줄인다

## 전략
감사(`design-concept/UI_CONSISTENCY_AUDIT.md`) §5 가 자른 단위를 그대로 받되,
**실행 축을 게이트 갈래에서 파일 구획으로 뒤집는다.**

**수치는 두 기준이 있다 — 이 문서는 래칫 기준을 쓴다.** 감사 정본은 제외분(생성물·복제물
15건)을 포함해 세고 게이트 래칫은 뺀다. 간격 **572 / 570**, 글자 **152 / 151** 이 그 차이다.
이 카드가 갚아야 할 것은 **게이트가 무는 수**이므로 아래는 전부 래칫 기준이고, 감사 문서를
인용할 때만 그쪽 수를 쓰며 그 사실을 함께 적는다. 섞어 쓰면 "줄었다"가 기준 차이인지
실제 청산인지 구별되지 않는다.

**왜 뒤집는가 — 갈래로 나누면 같은 파일을 여러 세션이 동시에 연다.** 게이트가 무는
1,395건(위반 1,305 + 드리프트 90)은 **81개 파일**에 있고, 그중 **74개가 두 칸 이상**에 걸린다
(칸 = `축 / 판정`, 래칫이 무는 단위. `baseline.ts:89`). 분포는 1칸 7 · 2칸 17 · 3칸 28 ·
4칸 16 · 5칸 7 · 6칸 4 · 7칸 1 · 8칸 1 이고 최다는 `pages/design/frame-picks.astro`(8) ·
`components/graph/GraphExplorer.tsx`(7) 이다. 판정을 접고 **축으로만 세도 73개가 두 축 이상**이다.
특히 글자 축 36파일 중 **35개가 간격 축 안에** 있어(차집합은 `deco/Tape.astro` 한 파일),
`KAN-072.4` 와 `KAN-072.5` 를 축별 세션에 주면 35파일에서 부딪힌다.
수치는 `bun scripts/verify-tokens.ts --json` 전건을 **기준선과 같은 정의로** 접어 낸 것이고,
칸 11개가 `scripts/tokens-baseline.json` 의 `counts` 와 전부 일치함을 확인했다.

**파일로 나누면 갈린다.** radius 축은 `src/components/posts/**` 에 **0파일**이고, 글자 축은
거기 **위반이 0**이다(드리프트 10건이 4파일에 남으므로 "글자가 발행물에 없다"고는 못 한다).
반대로 색 축은 위반 368건 중 **322건(87%)**이 거기 있다. 그래서 `posts/**` 밖을 구획으로
먼저 훑고 발행물은 시리즈 단위로 뒤에 간다.

**카드는 축으로 두고 배치가 파일을 나눈다** (유저 선택 2026-08-27). 카드 완료 판정이 게이트
칸(`축 / 판정`)과 1:1 로 맞아야 하기 때문이다 — 파일 구획 카드로 재편하면 "이 파일들의 모든
축이 0"이라는 판정을 게이트에서 뽑을 수 없다(`files` 맵은 실패했을 때 어느 파일이 늘었는지
지목하는 진단용이고 파일 단위 래칫이 아니다. `scripts/lib/tokens/baseline.ts:130`).
대가는 카드 완료가 뒤쪽 웨이브에 몰리는 것이고, 그 사이 진행은 게이트 카운트가 보여준다.

**첫 수는 치환이 아니라 게이트 인식층이다.** KAN-070 전략(`KANBAN.cards/KAN-070-DRF0QF.md:47`)은
"KAN-072.2 가 `--space-*`·`--text-*` 를 세우면 게이트는 문서를 아예 안 읽는다"를 전제로 코드와
데이터의 의존을 한 방향으로 정했다. 그런데 **그 전환이 구현되지 않은 채 카드가 닫혔다** —
`scripts/lib/tokens/color.ts:54` 의 `COLOR_TOKENS` 는 이름 18개가 고정이고, `:66` 은 `--stroke`
를 정확 일치로 보며, `docrule.ts:66,79` 의 `SPACING_SCALE`·`FONT_ROLES` 는 상수다. 신설할 토큰
6종 중 게이트가 자동으로 인식하는 것은 **radius 하나뿐**이다(`color.ts:64` 가
`includes("radius")` 라서 — 그 덕에 `50%` 25건은 토큰 한 줄로 닫힌다). 그래서
`KAN-072.7-D6DCXJ` 를 세워 그 층만 떼고, **게이트 코드 scope 는 그 카드 하나가 진다.**

**`KAN-072.3` 은 수행 없이 닫는다** (유저 선택 2026-08-27). KAN-070 의 `S5` 가 목표 두 줄을
이미 다 했다 — `docrule.ts:56-62` 가 26개 속성을 여백/치수/위치로 가르고 `:243-252` 가 뒤 둘을
「판정 불가」로 내리며, 감사 정본은 `UI_CONSISTENCY_AUDIT.md:260` 이 572 이고 `:268-279` 에
정정 블록이 붙었다. 카드 메모의 "검토자 572 · 재계산 574" 도 572 로 확정됐다(574 는 레포 전체에서
그 메모 한 곳에만 남은 폐기된 수다).

**제약 넷.**

1. **래칫은 축×판정 칸별로 물고 상쇄가 없다** (`baseline.ts:124`). `spacing / 위반` 을 100
   줄이면서 `color / 위반` 을 1 늘리면 그 커밋이 실패하고, 없던 칸이 새로 생기면 기준선이
   0 이라 그 자리에서 걸린다. **병렬 세션은 커밋마다 `bun run tokens:verify` 초록을 확인한다** —
   웨이브 끝에 몰아 보면 어느 세션이 늘렸는지 못 가른다.
2. **`--update-baseline` 은 자가검사·예외 검사 실패를 무시하고 exit 0 한다**
   (`verify-tokens.ts:98` 이 그 검사들보다 먼저 빠져나간다). 갱신 전에 반드시 맨몸으로 한 번
   돌린다. 게이트가 죽은 채 갱신하면 0 에 가까운 기준선이 박히고 그것이 곧 "다 갚았다"로 보인다.
3. **CI 는 `push: main` 트리거라 브랜치에서 안 돈다**(`KANBAN.cards/KAN-070-DRF0QF.md:182`).
   병합 전까지 로컬 확인이 유일한 그물이다.
4. **`verdict` 예외는 파일 단위이고 값을 못 가른다** — `color.ts:230` 이 그 파일의 색 히트를
   통째로 「정당한 예외」로 내린다("이 파일의 이 3색만"은 지금 구조로 불가능하다). 그래서
   마스코트 28건은 예외 등록이 아니라 팔레트를 토큰·문서에 박는 쪽으로 간다(감사 P2-2 와 같은 방향).

**버린 대안 셋.**

- **축별 병렬(감사 §5 의 사슬을 그대로 세션에 매핑)** — 위 75/82 겹침으로 그 자리에서 깨진다.
- **파일 구획 카드로 재편** — 완료 판정이 게이트 칸과 안 맞고, 카드 6장에 쌓인 브리프·메모
  근거를 옮겨 적어야 한다.
- **게이트 코드를 KAN-072 scope 로 흡수** — `KAN-072.2` 하나가 토큰 데이터와 판정 코드를
  동시에 소유하게 되어 KAN-070 이 세운 분리가 그 자리에서 깨진다.

---

**부채는 배치2에서 1,395 → 1,786 으로 드러났다 (2026-08-27).** 늘어난 것이 아니라 **보이게 된
것**이다 — 토큰이 없어서 「문서 스케일 안이니 준수」로 세어지던 392건이, 토큰이 서자
「토큰이 있는데 리터럴로 적었다」로 정확히 재분류됐다. 히트 단위 전이는 위반→드리프트 542 ·
준수→드리프트 392 · 판정불가→드리프트 63 이고 **느슨해진 방향은 1건**뿐이다
(`PostList.astro:321` 의 `margin-left: -2px` — 2px 이 정식 눈금이 되어 닫혔다).
아래 구획 무게는 그 뒤 기준선이다.

**수행 방식은 오케스트레이션이다 (유저 선택, 2026-08-27).** 배치 7개 · 병렬 폭 최대 3.
work 하나 = 파일 구획 하나이고, 구획마다 **그 파일의 모든 축**을 한 세션이 처리한다.
구획 경계가 곧 세션 경계라 두 세션이 같은 파일을 여는 일이 없다.

구획과 무게(게이트가 무는 건수, `scripts/tokens-baseline.json` 의 `files` 맵 기준):

| 구획 | 파일 | 건수 |
|---|--:|--:|
| 카탈로그 `pages/design/**` | 3 | 285 |
| 그래프 `components/graph/**` | 1 | 94 |
| 데코 `components/deco/**` | 18 | 173 |
| 글 컴포넌트·레이아웃·페이지·셸 | 23 | 375 |
| viz·전역 스타일 | 2 | 68 |
| `posts/**` osi-7-layers | 12 | 329 |
| `posts/**` vpn-anatomy | 9 | 149 |
| `posts/**` webrtc | 5 | 131 |
| `posts/**` auth-authz | 4 | 80 |
| `posts/**` container·tauri·orca | 5 | 68 |
| 마스코트 `motifs/**` | 1 | 34 |
| **합계** | **83** | **1,786** |

**구획 무게는 배치가 끝날 때마다 다시 잰다.** 토큰이 서면 판정이 재분류되므로 착수 시점의
수와 계획 시점의 수가 다르다 — 각 배치의 「착수 시점 판단」이 그때의 수를 적는다.

## 실행 계획
- [x] `S1` 게이트 인식층 — 토큰 이름 규칙을 데이터로 (`KAN-072.7`)
- [x] `S2` 토큰 신설 — `tokens.css` + `DESIGN_CONCEPT` §5·§8·§9 (`KAN-072.2`)
- [x] `S3` 카탈로그 구획 — `pages/design/**` 3파일 285
- [x] `S4` 그래프 구획 — `components/graph/**` 1파일 94
- [x] `S5` 데코 구획 — `components/deco/**` 18파일 173
- [x] `S6` 글 컴포넌트·레이아웃 구획 — 13파일 284
- [x] `S7` 페이지·셸 구획 — 10파일 91 (`AuthorLineup` 흡수)
- [x] `S8` viz·전역 스타일 구획 — 2파일 68
- [x] `S9` osi-7-layers EP1~3 — 6파일 (배치5 착수 시 재분할. 시리즈 합 329)
- [x] `S10` osi-7-layers EP4~6 — 6파일 (배치5 착수 시 재분할)
- [x] `S11` vpn-anatomy — 9파일 149
- [ ] `S12` webrtc — 5파일 131
- [ ] `S13` auth-authz — 4파일 80
- [ ] `S14` container-anatomy · tauri · orca-dev-env — 5파일 68
- [ ] `S15` 마스코트 팔레트 — `motifs/Mascot.astro` 34
- [ ] `S16` 수렴 확인 — 기준선 0 · 카드 6장 완료 판정

**구획은 겹치지 않고 합이 전부다.** 13개 구획의 파일 수 합이 **83**, 건수 합이 **1,786** 으로
`scripts/tokens-baseline.json` 의 `files` 맵 총계와 같다. 구획 경계가 곧 세션 경계라
두 세션이 같은 파일을 여는 일이 없다.

**수치는 배치2 뒤 기준선이다.** 토큰이 서면서 준수 392건이 드리프트로 재분류돼 부채가
1,395 → 1,786 으로 드러났고, 파일도 81 → 83 이 됐다. 배치가 끝날 때마다 다시 재므로
각 구획의 정확한 수는 그 배치의 「착수 시점 판단」이 적는다.

**단계별 완료 기준**

- `S1` — `tokens.css` 에 `--stroke-hair`·종이흰색·`--state-*`·`--space-*`·`--text-*` 를 임시로
  넣은 워킹트리에서 게이트가 그것들을 **자기 축으로** 판정한다(지금은 전부 `other` 로 샌다).
  자가검사 6종이 그대로 각자 다른 사유로 걸리고, 판정 문구를 바꿨다면 `selftest.ts:32-39` 의
  기대 조각도 함께 갔다. **판정 수는 안 바뀐다** — 인식층만 여는 단계라 이 work 는 위반을
  하나도 안 줄인다(줄면 규칙이 헐거워진 것이므로 되돌아본다).
- `S2` — 토큰이 서고 `DESIGN_CONCEPT` §8 에 선 굵기 스케일과 `50%` 허용이, §9 에 간격 토큰
  이름이, §5 에 글자 토큰 이름이 적힌다. **여기서 radius 위반 25건(`50%`)이 닫힌다** —
  `color.ts:64` 가 이름에 `radius` 가 들어간 토큰을 자동으로 그 축에 넣기 때문이다.
  `1px` 리터럴 129건은 「위반」에서 「드리프트」로 재분류된다(줄지는 않는다 — 치환은 뒤 구획).
  판단 다섯을 이 단계 착수 시점에 유저가 정한다(배치2 문서 4항).
- `S3`~`S15` — 각 구획의 게이트 건수가 0 이고, **다른 축 칸이 하나도 안 늘었다**(래칫은 칸별로
  물고 상쇄가 없다 — `baseline.ts:124`).
  `bun run tokens:verify` 가 exit 0 이며 커밋 뒤 `--update-baseline` + 기준선 커밋까지 한 단위다.
  `posts/**` 구획(`S9`~`S14`)은 여기에 하나가 더 붙는다 — **그 글을 `astro preview` 로 열어
  시뮬 인터랙션이 도는 것을 확인한다**(감사 P2-1: 발행물의 부품이라 빌드만으론 부족하다).
- `S15` — 마스코트 색 28건이 팔레트로 정리되고 그 팔레트가 `DESIGN_CONCEPT` §7 이나
  `--tony-*` 토큰에 적힌다. **`verdict` 예외로 걸지 않는다** — `color.ts:230` 이 파일 단위라
  그 파일의 색 히트가 통째로 빠져 다섯 번째 분홍이 들어와도 안 잡힌다.
- `S16` — 기준선의 `위반`·`드리프트` 전 칸이 0 이고, 하위 카드 6장이 각자의 축 칸 0 으로
  완료 판정을 받는다. 하드월 승격은 이 카드가 안 한다 — 게이트 코드는 KAN-070 소유다
  (`verify-tokens.ts:20`, `main.yml:73-74`).

## 검증
1. **게이트가 0 을 낸다** — `bun run tokens:verify` 가 `위반`·`드리프트` 전 칸 0 을 보고하고
   exit 0. `scripts/tokens-baseline.json` 이 그 수로 커밋돼 있다.
2. **게이트가 살아 있다** — 자가검사 6종이 각자 다른 사유로 걸린다. 리터럴 한 줄을 일부러
   더한 워킹트리에서 비0 + 그 `파일:줄` 을 지목하고, 지우면 0.
   **이 항이 1번보다 먼저다** — 죽은 게이트와 다 갚은 게이트는 화면에서 같은 모양이다.
3. **회귀 없음** — `bun run build` · `bun scripts/check-post-markers.ts` ·
   `bun scripts/check-emphasis.ts`(+`--dist`) · `bun run viz:verify` · `bun run deco:verify` ·
   `bun run width:verify` · `bun run talk:verify` · `bun run graph:verify` · `tsc --noEmit` 전부 통과.
   `bun run gen:motion` 후 `git diff --exit-code -- src/styles/motion.css` 통과.
4. **발행물이 안 깨졌다** — `posts/**` 를 건드린 글을 `astro preview` 로 열어 시뮬 인터랙션이
   돈다. 빌드만으로는 안 잡힌다.
5. **화면이 안 바뀐 것이 확인됐다** — 주요 지면(홈 · 글 목록 · 글 상세 · 글 지도 · 대담 ·
   `/design/deco`)을 웨이브 전후로 대조한다. 바뀐 자리는 **왜 바뀌었는지가 수행 내역에 적혀
   있다** — "화면 변화 0"이 목표지만 스케일 스냅은 값을 움직이는 일이라 0 을 단정하지 않는다.
6. **다음 리터럴이 막힌다** — 리터럴을 넣은 브랜치를 만들어 `tokens:verify` 가 무는 것을 본다.
   CI 는 `push: main` 트리거라 브랜치에서 안 도므로 로컬로 확인한다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-08-27T01:09 · s:29c42810 — `전략` 섹션 교체
- 2026-08-27T01:09 · s:29c42810 — `실행 계획` 섹션 교체
- 2026-08-27T01:09 · s:29c42810 — `검증` 섹션 교체
- 2026-08-27T01:44 · s:29c42810 — `전략` 섹션 교체
- 2026-08-27T01:46 · s:29c42810 — `실행 계획` 섹션 교체
- 2026-08-27T01:49 · s:29c42810 — KAN-072.3(축 재분류) 수행 없이 완료 — KAN-070 S5 로 흡수됐다. docrule.ts:56-62 가 26개 속성을 여백/치수/위치로 가르고 :243-252 가 뒤 둘을 판정 불가로 내린다. 감사 정본도 UI_CONSISTENCY_AUDIT.md:260 이 572, :268-279 에 정정 블록. 확정치 572, 폐기된 574 는 카드 메모에서 제거
- 2026-08-27T01:49 · s:29c42810 — 착수 — orca 워크트리 /Users/centurio/orca/workspaces/centurio1987.github.io/KAN-072-CPJCT1 · 브랜치 KAN-072-CPJCT1 (base main @997edf6). 계획 리포트 발행: https://claude.ai/code/artifact/89712866-9024-4c9e-ae18-b8df83c2ba58
- 2026-08-27T01:50 · s:29c42810 · S1 doing — 착수
- 2026-08-27T02:10 · s:29c42810 · S1 done — 게이트 인식층 개통. 축 판별을 scripts/lib/tokens/axis.ts 단일 소스로 빼고 하드코딩 넷을 규칙으로 바꿨다 — 색은 이름 18개 Set 대신 값이 색인지로(--cat- 접두는 유지), stroke 는 정확일치 대신 --stroke 접두, 간격·글자 스케일은 tokens.css 의 --space-*/--text-* 에서 파생(토큰 0개면 문서 상수 폴백). 판정 전건 3,647행을 9쪽으로 뽑아 변경 전후 diff 0, 기준선 11칸 불변. 임시 토큰 5종 투입 시 각자 축으로 인식되는 것을 실측 확인 후 되돌렸다. 자가검사 6종 생존, 색 규칙을 일부러 죽이면 사유 5가지로 걸린다. --hatch 는 그라디언트라 color→other 로 내려가나 대조 상대가 된 적이 없어 영향 0건, deco.css 색 44개는 other→color 로 올라가나 새 드리프트 0건
- 2026-08-27T03:01 · s:29c42810 · S2 doing — 착수
- 2026-08-27T03:03 · s:29c42810 — S2 착수 중 S1 누락 발견 — docrule.ts:89 의 FONT_MIN=12 가 상수라 --text-* 에 10·11 을 세워도 게이트가 여전히 '문서 최소치 미만 위반' 으로 판정한다. 스케일 집합만 토큰 파생으로 바꾸고 하한은 안 바꾼 것이 원인이다(S1 지시에 안 넣었다). 게이트 코드라 KAN-072.7 범위이고, work 를 다시 열지 않고 S2 에서 함께 처리한다
- 2026-08-27T03:24 · s:29c42810 · S2 done — 토큰 25종 신설(44→69) + DESIGN_CONCEPT §5·§8·§9 개정. 종이흰색 --surface-hi #fffdf8 · --stroke-hair 1px · --radius-round 50% · --space-* 13종(2px 반 단 포함) · --text-* 9종(Micro 11 · Nano 10 신설). --state-* 는 배치5로 미룸. 게이트 보강 둘을 함께 처리했다 — FONT_MIN 을 역할값 최소로 파생(S1 누락분, 하한이 10px 로 따라온다) · 드리프트가 §9 축 관할을 보게 가드(치수·좌표 63건이 --space-* 와 값이 같다는 이유로 다시 물리던 것을 막는다. KAN-070 S5 의 축 재분류를 드리프트 쪽에도 일관 적용). 재현 검증 821·166 이 깨진 것도 함께 고쳤다(드리프트가 스케일보다 먼저 판정되는 탓). 결과: 위반 1305→762 · 드리프트 90→1024 · 래칫 대상 1395→1786. 전이는 위반→드리프트 542 · 준수→드리프트 392 · 판정불가→드리프트 63 이고 느슨해진 방향은 1건(PostList.astro:321 margin-left -2px). build 46쪽 · deco:verify 50회 · viz:verify 43개 통과. 기준선 갱신 후 게이트 통과
- 2026-08-27T03:25 · s:29c42810 — `실행 계획` 섹션 교체
- 2026-08-27T13:56 · s:29c42810 · S3 doing — 착수
- 2026-08-27T13:56 · s:29c42810 · S4 doing — 착수
- 2026-08-27T13:56 · s:29c42810 · S5 doing — 착수
- 2026-08-27T14:47 · s:29c42810 — 배치3 1차 실행에서 §8(radius·선 굵기)만 안 넓힌 것이 화면에 드러났다 — 세 세션이 독립적으로 같은 지적을 했다. radius 최소가 8px 이라 1~7px 22건이 8px 로 부풀고(카탈로그 W1 크라프트 택 34×24 가 각진 택에서 둥근 라벨로), 선 굵기가 1/1.5 두 단이라 2px 23건·3px 8건이 1.5px 로 얇아진다(인용 왼쪽 분홍 바·종이 클립 외곽선). 실측 지배값은 1px 205 → 2px 23 → 1.5px 19 순이라 2px 이 빠진 2단은 현실을 못 담는다. 유저 결정: --radius-xs 4px · --stroke-bold 2px 를 세우고 배치3 을 되돌려 다시 돈다(커밋 전이라 git checkout 하나)
- 2026-08-27T14:47 · s:29c42810 — 배치3 1차에서 실측으로 잡은 회귀 — 재실행 때 다시 겪는다. Tape.astro 레터링을 9px→10px(--text-nano) 로 올리면 홈 「최근 글」 머리의 BAKED FRESH 테이프에서 마지막 H 가 잘린다. .tape 는 width:var(--w)(호출부 104px)에 clip-path 가 자식까지 자르는데 라벨이 97.7→108.6px 로 넘친다. 부품이 스스로 맞추게 .letter { min-width: max-content } 로 고쳤다(호출부 index.astro:91·design/deco.astro:373 은 구획 밖). width: max(var(--w), min-content) 는 안 된다 — min-content 가 max() 인자로 무효라 모든 레터링 테이프가 글자 폭으로 쪼그라든다(156.5→111 실측)
- 2026-08-27T14:47 · s:29c42810 — 배치3 1차 기록 — Tape.astro:102-103 이 쓴 토큰: border-top 은 var(--stroke-hair) solid color-mix(in srgb, var(--surface-hi) 70%, transparent), border-bottom 은 같은 굵기에 color-mix(in srgb, var(--deco-kraft-ink) 16%, transparent). viz-frame.css:285-286 이 같은 값을 복제하고 있으므로 배치4 의 viz 구획이 이것을 따라야 복제가 없어진다 — 다만 그쪽은 전역 시트라 데코 토큰에 fallback 이 필요하다(var(--deco-kraft-ink, #6b5a3e))
- 2026-08-27T15:00 · s:29c42810 — 배치4 로 넘길 것 — src/pages/graph.astro 두 자리가 GraphExplorer 치환으로 낡는다. :513 의 is:global 규칙이 .graph-explorer svg 의 border·radius 를 0 으로 덮어 그 컴포넌트 선언이 죽은 값이 되고(토큰화는 했다 — 지면이 안 덮으면 살아나는 기본값이라), :562 는 .graph-help 의 padding-top 을 32px 로 덮으면서 주석에 '(원래 11px)' 를 적어 뒀는데 그 11px 이 이제 --space-12 다. graph.astro 를 맡는 구획이 그 주석을 함께 정정한다
- 2026-08-27T15:17 · s:29c42810 · S3 done — 카탈로그 285→0. 172건은 값 동일(var() 치환만) · 113건 스냅, 동률은 전부 작은 쪽. §8 확장 효과 둘 — .w1-tag radius 2px→--radius-xs(4)로 각진 택 유지(1차 8px), .lede.warn border-left 3px→--stroke-bold(2)로 색 막대 유지(1차 1.5px). .mark-stage 는 리터럴 28px(토큰이면 clamp 상한 38×1.15em=43.7px 로 44px 스테이지를 꽉 채운다, 주석 기재). 가로 오버플로 0(3페이지×1280/390) · deco:verify 50회 · 치수·좌표 66건 불변
- 2026-08-27T15:17 · s:29c42810 · S4 done — 그래프 94→0. 전부 컴포넌트 안 <style> 블록 CSS 였고 inline style·SVG 속성 히트 0. 토큰 매칭 64 · 스냅 18 · 죽은 fallback 제거 4. §8 확장 효과가 여기서 가장 크다 — 선 굵기 30곳 전부 값 유지(1차엔 밀렸다), --stroke-bold 가 서면서 1.5px 이 3단 중간이 됐다. --radius-xs 는 이 파일에서 한 번도 안 쓰였다(8px 미만 후보가 없었다 — 토큰은 모든 구획에서 쓰이는 게 아니라 필요한 곳에서 값을 지키는 것이 목적이다). 검증: 변경 전후 두 빌드로 5개 상태 × 244요소 × 35속성 전수 diff, var() 무효 0 · 콘솔 에러 0 · border-width 항목 0 · 게이지 드래그 동일
- 2026-08-27T15:17 · s:29c42810 · S5 done — 데코 173→0. DECO_KIT 규약 셋 이행(color-mix 6곳 · Doodle 살아있는 fallback 3곳 시안 hex · 종이흰색 14곳 --surface-hi 통합). §8 확장 효과 — 인용 분홍 바·클립 외곽선 3px→--stroke-bold(2), Clothespin 몸통 4/4/6/6 이 --radius-xs 하나로(1차엔 8px 라 위 모서리까지 배로 둥글었다). Tape 레터링 회귀 재현·차단(.letter min-width:max-content, 라벨 108.56 vs 테이프 105.84→109.95). TicketStub 노치는 calc(var(--space-14)/-2) 로 토큰에서 뽑았다. 자기 18파일만 HEAD 로 되돌린 사본을 따로 구워 대조 — 가로 스크롤 변화 0 · 새로 잘린 요소 0(부품 300여 개 전수) · deco:verify 50회 · 치수·좌표 187곳 불변
- 2026-08-27T15:18 · s:29c42810 — 정정 — 배치3 재실행 지시에 내가 넣은 'width: max(var(--w), min-content) 는 min-content 가 max() 인자로 무효라 모든 레터링 테이프가 쪼그라든다' 는 틀린 근거다. 데코 세션이 실제로 넣어 재니 이 크로미움에서 108.125px 로 계산돼 무효로 안 떨어졌다. 1차의 쪼그라듦(156.5→111)은 다른 원인이었다. 결론(.letter min-width:max-content 를 쓴다)은 유지한다 — 문구에 공백이 있으면 마지막 낱말이 떨어지고 브라우저별로 갈리는 자리라서다
- 2026-08-27T15:18 · s:29c42810 — §9 는 넓히지 않기로 했다(내 판단). 16~24 사이 여백이 70건(18px 32 · 22px 20 · 20px 17 · 17px 1)이라 --space-20 후보가 올라왔지만, §8 과 성격이 다르다 — §8 은 구멍이었다(지배값 2px 선에 이름이 없어 강조가 통째로 사라지고 radius 최소 8px 이 34×24 택을 둥글게 만들었다). §9 의 16→24 도약은 설계다: 큰 여백일수록 성기게 가는 것이 스케일의 통상 형태이고, 20px 이 16 으로 가는 것은 스케일이 값을 모으는 정상 동작이다. 대가는 카탈로그 .filter-card 하나의 비대칭이 Δ2→Δ8 로 벌어지고 카드 높이가 78→70px 이 되는 것
- 2026-08-27T15:18 · s:29c42810 — 배치3 종료 — 552건 닫힘(1786→1234). 기준선 갱신 후 게이트 통과, 자가검사 6종 생존. 커밋은 구획별로 셋(dba42cb 카탈로그 · 4d5f587 그래프 · 428505d 데코) — 구획 하나만 되돌릴 수 있게. 다음은 배치4(S6·S7·S8, 병렬 3): 글 컴포넌트·레이아웃 · 페이지·셸 · viz·전역 스타일
- 2026-08-27T15:25 · s:99b49ba0 — `실행 계획` 섹션 교체
- 2026-08-27T15:25 · s:99b49ba0 · S6 doing — 착수
- 2026-08-27T15:25 · s:99b49ba0 · S7 doing — 착수
- 2026-08-27T15:25 · s:99b49ba0 · S8 doing — 착수
- 2026-08-27T16:15 · s:99b49ba0 · S6 done — 글 컴포넌트·레이아웃 284→0(13파일). 값 동일 176 · 스냅 106. 값 동일 중 6은 위반인데 렌더 결과가 같은 자리다(pill radius 넷은 높이보다 반지름이 커서 전후 모두 완전 pill, .toc-tick 은 높이 2px 라 1px→4px 이 실제 1→1). .prose blockquote 는 MarkedQuote 가 덮어 실측 화면 변화 0 이지만 S5 가 옮긴 값과 맞춰 인용이 두 얼굴이 되지 않게 했다. 유저 판정 셋 반영 — 대담 제목 clamp(30,6vw,46)→clamp(28,6vw,38)(램프는 6vw 유지, var(--text-h1) 은 5vw 라 지면마다 자라는 속도가 갈린다) · --text-small 로 되돌린 자리 여섯 · .related-title 26→--text-h3(20). .qa-text 만 --text-body 로 남겼다(16.5 는 15/18 동률이나 그 자리는 대담 본문이고 §5 대전제가 역할 우선이다). 검증: 자기 13파일만 HEAD 로 되돌린 사본을 따로 구워 Playwright 전수 대조, 지면 5종×폭 3종 가로 오버플로 0 · 새로 잘린 요소 0. deco:verify·width:verify 는 --base 로 자기 빌드를 물려 다시 돌렸다(기본값이 공유 dist 라 처음엔 낡은 빌드를 재고 있었다)
- 2026-08-27T16:16 · s:99b49ba0 · S7 done — 페이지·셸 91→0(10파일). 값 동일 55 · 스냅 35 · 죽은 규칙 삭제 1. AuthorLineup.astro(1건)를 흡수했다(계획 때 81파일 기준선에 없던 파일 — 배치2 재분류로 드러났고 푸터 라인업이라 Footer 와 같은 세션이 연다). 배치3 인계 두 자리를 닫았다 — graph.astro 의 is:global 이 덮어 죽은 값이 된 GraphExplorer 선언은 override 를 정본으로 두고 주석에 박았고, .graph-help 의 '(원래 11px)' 주석은 인계 메모가 --space-12 라 한 것이 틀려 --space-10 으로 정정했다(원본이 padding: 11px 13px 이고 문제되는 것은 padding-top 이라 11 은 세로값이다). 값이 크게 움직인 자리 셋: 404 제목 64px→--text-display(390px 에서 36px, 유저 판정으로 유지 — 64 는 Display 상한 60 보다도 큰 유일한 값이라 스케일 위에 홀로 서 있었다) · index.astro 도장 여유 26.2→16.2px · /posts 요약 여유 20→10px(겹침 띠 안에서는 21.3px). calc(var(--space-64)+var(--space-10)) 로 74px 을 보존하는 길은 안 썼다 — 스케일 밖 값을 조합으로 되살리는 것이라 §9 판정과 어긋나고 게이트 우회로가 된다. 검증: 정적 서버 + Playwright 로 computed style·박스·Range 기반 글자 오른쪽 끝을 before/after diff, 지면 6종×폭 4종 가로 스크롤 회귀 0
- 2026-08-27T16:16 · s:99b49ba0 · S8 done — viz·전역 스타일 68→0(2파일). 값 동일 44 · 스냅 23 · 값 제거 1. 감사 §3-9 의 복제 둘을 닫았다 — viz-frame.css:285-286 이 Tape.astro:102-103 을 복제하던 것을 배치3 표현으로 맞추되 fallback 은 --deco-kraft-ink 에만 달았고(--surface-hi 는 :root 라 전 페이지에 있고 --deco-* 는 opt-in 이라 대담엔 없다), 액자 종이색 #fffcf6 두 자리를 --surface-hi 로 모았다(채널당 최대 2 이동). margin-left:-39px 는 스냅 대신 없앴다 — -39 는 간격이 아니라 폭 78 의 절반이라 --space-32 로 밀면 마테가 7px 떨어지고, translateX(-50%) 로 바꾸니 행렬이 tx=-39 로 그대로다. viz-frame.css:419 의 inset 19/17px 은 게이트가 안 무는 좌표지만 주석이 '테두리 1px + 패딩' 이라 못박은 파생값이라 calc(var(--space-16)+var(--stroke-hair)) 로 함께 옮겼다(안 옮기면 F-D 홀더가 2px 뜬다). 유저 판정으로 pre·table 을 --text-small 로 되돌렸다 — 최근접은 13/18 이었으나 code{0.875em} 이 겹쳐 실제 코드 글자가 11.375px(Label 아래)이 되고 표 폭이 510→561px 로 벌어졌다. DIAGRAM_STYLE_GUIDE.md:21 은 자기 범위를 (blogVizStyleGuide canvas.bg) 로 적어 SVG 캔버스 배경이지 CSS 액자 판이 아니라 어긋나지 않는다. 검증: 자기 2파일만 HEAD 로 되돌린 짝으로 10지면×2폭 풀페이지+요소 실측+픽셀 대조, 가로 스크롤 회귀 0. 홈·graph 픽셀 차는 같은 빌드를 두 번 찍어도 더 크게 나온다(마스코트 스윙·그래프 렌더 잡음)
- 2026-08-27T16:16 · s:99b49ba0 — S2 보강 — §5 를 넓혔다(--text-small 15px). 배치3 이 §8 을 넓힌 것과 같은 경위다: 배치4 가 화면으로 드러냈고 유저가 판정했다(2026-08-27). 근거는 Body(18)~Meta(13) 사이 5px 빈칸에 23건이 앉아 있었고(17px 8 · 15px 5 · 14px 4 · 16px 2 · 16.5px 1 · 13.5px 2) 그중 절반이 부속 정보가 아니라 읽는 글이었다는 것 — 코드블록은 code{0.875em} 이 겹쳐 실제 글자가 13.125→11.375px(Label 12 아래)이 됐고 글 목록 설명문은 390px 에서 4줄이 3줄이 됐다(문서 높이 -8.2%). 반대로 §9 는 배치3 에서 안 넓혔다. 구멍과 설계를 가르는 것은 빈칸의 폭이 아니라 그 자리에 실재하는 쓰임이다. 다만 이 구간 23건은 배치4 구획에만 있고 posts/** 에는 0건이라, 이 칸이 값을 하는 것은 이 배치까지다(그 사실도 유저에게 근거로 내고 골랐다). S1 인식층 덕에 게이트 코드 변경 0 — --text-* 9종이 10종으로 자동 인식됐다. 동률 작은 쪽 규약은 그대로라 14px 4건은 Meta 에 남는다
- 2026-08-27T16:16 · s:99b49ba0 — 배치4 종료 — 443건 닫힘(1234→791). 파일 61→36, 래칫 칸 11→5(radius·stroke 축 전멸, font 위반 0). 커밋은 여섯: f608d6f 토큰 신설 · 980ba92 S6 · f1cca62 S7 · 11551b7 S8 · 06cf7dd 부수 정리 · 기준선. 구획 하나만 되돌릴 수 있게 나눴다. 부수 정리는 세 세션이 구획 밖이라 못 건드린 셋이고 전부 게이트가 안 무는 자리다(래칫 대상은 위반·드리프트뿐 — baseline.ts:31): GraphExplorer 의 죽은 선언 3줄 · VizFigure:87 의 낡은 주석 · deco.css 의 종이흰색 잔여 2자리(filter:drop-shadow() 안이라 배치3 추출층에 안 잡혔다). 검증 — 게이트 exit 0 · 자가검사 정상 1 + 고장 6종 사유 6가지 · build 46쪽 · tsc(고친 파일 에러 0) · deco/viz/width/talk verify · check-post-markers · check-emphasis(+--dist) · gen:motion 후 motion.css 드리프트 0. graph:verify 만 실패하는데 배치와 무관한 선행 상태다(graph.json 이 글 4편을 안 담고 있다 — src/data/·src/content/ 변경 0건이고 재생성은 main 워크트리 전용이라 여기서 안 한다). 다음은 배치5(S9·S10·S11 — osi-7-layers 12파일 · vpn-anatomy 9파일), 착수 시 구획 무게를 다시 잰다
- 2026-08-27T16:16 · s:99b49ba0 — 병렬 3 에서 실제로 겪은 것 둘 — 배치5 부터 지시에 넣는다. ① 스크래치패드 이름 충돌: 세 세션이 measure.mjs·shot-before/ 를 같은 이름으로 쓰다 S7 의 측정 스크립트가 한 번 죽었고, S8 이 S6 의 shot-before/ 를 자기 것으로 착각해 지웠다(S6 는 이미 대조를 끝낸 뒤라 결과에는 영향 없음). 세션마다 scratchpad/<work>/ 네임스페이스를 지정한다. ② 같은 워크트리에서 찍은 전후 대조는 못 믿는다: S8 의 첫 짝은 그 사이 S6 가 Header·Footer 를 고쳐 홈 -49px · 글 목록 -483px 이 S8 몫으로 잡혔고 실제 S8 변경분은 0 이었다. 자기 파일만 HEAD 로 되돌린 사본을 따로 구워 대조하는 S5·S6 방식이 정답이고 그것을 지시에 못박는다. ③ deco:verify·width:verify 는 기본값이 공유 dist 라 병렬 중에는 낡은 빌드를 잰다 — --base 로 자기 빌드를 물려야 한다
- 2026-08-27T16:16 · s:99b49ba0 — 내가 낸 고장 하나 — GraphExplorer.tsx 의 죽은 선언을 지우며 넣은 주석에 백틱을 써서 템플릿 리터럴을 닫아 버렸고 tsc 가 1899줄부터 파싱 에러를 냈다. 그 CSS 는 JS 템플릿 리터럴 안이라 주석에도 백틱을 못 쓴다. 백틱을 빼고 그 사실을 같은 주석에 남겨 다음 사람이 다시 겪지 않게 했다. 빌드가 아니라 tsc 가 먼저 잡았다
- 2026-08-27T16:27 · s:4abb90c0 · S9 doing — 착수
- 2026-08-27T16:27 · s:4abb90c0 · S10 doing — 착수
- 2026-08-27T16:27 · s:4abb90c0 · S11 doing — 착수
- 2026-08-27T19:18 · s:4abb90c0 · S9 done — osi EP1~3 171→12(6파일). 값 동일 21 · 스냅 77 · 제거 1(죽은 const SAND) · 선굵기 27 · 간격 73. 유저 판정대로 잉크를 사이트 팔레트로 통합했다 — 본문 31자리 rgb(48,45,40)→rgb(32,38,74) · 부속 16자리 rgb(107,99,87)→rgb(74,79,106) · 테두리 9자리. 기하 변화는 RoutingTableLab 액자 -2px 하나뿐(IP 입력 상하 패딩 5→4). #5c5648(바이트 라벨)은 매핑표 변종이라 --ink-2 로 함께 갔다(Δ39, 유저가 승인한 #6b6357 Δ43 과 같은 크기) — 10.5px 라벨이라 줄바꿈을 실측했고 1280·390px 10개 상태에서 버튼·액자 높이 0px 변화. 검증: 격리 사본(자기 6파일만 HEAD)으로 46쪽 중 달라진 쪽 정확히 3편 · 오버플로 0 · 인터랙션 20개 상태 var() 무효 0. 남은 12는 토큰이 없어 멈춘 자리(프레임 필드 6색 · #9a5b2c 3 · #eef5f3 · #c0392b 2)
- 2026-08-27T19:18 · s:4abb90c0 · S10 done — osi EP4~6 158→5(6파일). 값 동일 75 · 스냅 92. 잉크 통합으로 글자 49 + 물려받은 테두리 31 · 캡션 36 · 선 29 가 움직였고, 간격 동률은 전부 작은 쪽(3→2 · 5→4 · 9→8). 문서 높이는 EP4·EP5 0, EP6 -12px. 되돌린 것 하나 — 판정 대기 리터럴을 const 로 끌어올리면 게이트 시야를 벗어난다. 추출층이 prop:"값" 꼴만 봐서 const WARN="#9a5b2c" 는 통째로 안 잡히고, 그래서 158→0 이 '닫힌 것'과 '숨은 것'을 섞고 있었다(실측: 토큰으로 닫힘 152 · 시야 밖 6). #9a5b2c 5건을 원래 인라인 자리로 되돌려 게이트가 다시 물게 했다 — S9 이 같은 값 3건을 시야 안에 두고 있어 그래야 시리즈가 한 상태가 된다. 되돌린 자리에 왜 상수로 안 올리는지 주석으로 박았다. 원래부터 게이트 시야 밖이던 5값(#b8b0a0·#e8c97a·#cfe3da·#fff3d6·#e5f0ed)은 상수 유지 — 수치를 왜곡하지 않고 근거 주석과 함께 모인 만큼 개선이다. #5c5648 은 S9 판정에 맞춰 --ink-2 로(내 지시 누락으로 한 번 갈렸던 자리). 검증: uid 제거 후 dist 전체 diff 로 달라진 쪽 정확히 3편 · 인터랙션 6종 양쪽 상태
- 2026-08-27T19:18 · s:4abb90c0 · S11 done — vpn-anatomy 149→4(9파일). 값 동일 54 + 알파 43(color-mix, 오차 ≤0.0005) · 스냅 60 · 2차 판정 8. 상태색은 물을 것이 없었다 — const OK=#3E6B4F 가 --cat-planning 과, DANGER/ACCENT/POP 가 각각 토큰과 값이 정확히 같아 var() 치환만 했다. 배치2 가 미룬 --state-* 는 세우지 않는다(같은 값에 이름이 둘이 된다). 실제 화면 변화 359자리 중 독자가 알아보는 것은 잉크 통합 223자리뿐이고 나머지는 ΔRGB 11 이하. 레이아웃은 7편 중 4편만 움직였다(EP7 -14 · EP4 -12 · EP5 +1~2 · EP3 -1~4). 인터랙션 실패 2건은 컴포넌트가 아니라 하네스였다 — client:visible 아일랜드가 수화되기 전 클릭이 죽은 마크업에 떨어진다. 대상 figure 의 조상 아일랜드 하나만 기다리게 고치니 9개 시뮬 42스텝 실패 0(전체 astro-island[ssr] 소멸을 기다리면 화면에 안 들어오는 섬 때문에 타임아웃난다). 하네스 자가검사 — --ink 값 주입 시 473자리 검출. 남은 4는 FATE_META 상태 테두리 4색(서로 구분되는 한 벌이라 하나만 옮기면 짝이 깨진다)
- 2026-08-27T19:21 · s:4abb90c0 — 배치5 종료 — 457건 닫힘(791→334). 파일 36→24, 구획 셋 478→21. 커밋은 구획별로 셋(a8ae29d S9 · 19bcc79 S10 · 522b9b3 S11) — 글 하나 단위로 되돌릴 수 있게. 검증: tokens:verify exit 0(자가검사 정상 1 + 고장 6종 사유 6가지) · build 46쪽 · viz/deco/width/talk verify · check-post-markers · check-emphasis(+--dist) · gen:motion 후 motion.css 드리프트 0 · tsc 21파일 에러 0. graph:verify 만 실패하는데 배치와 무관한 선행 상태다(graph.json 에 orca-dev-env 가 없다 — src/data·src/content 변경 0건이고 재생성은 main 워크트리 전용). 다음은 배치6(S12·S13·S14 — webrtc 5파일 · auth-authz 4파일 · container/tauri/orca 5파일), 착수 시 구획 무게를 다시 잰다
- 2026-08-27T19:21 · s:4abb90c0 — 배치5 유저 판정 — 시뮬 잉크를 사이트 팔레트로 통합한다(2026-08-27). 감사 UI_CONSISTENCY_AUDIT.md:223 이 「본문 잉크가 두 벌이다」로 지목한 자리다. #302d28→--ink(Δ38) · #6b6357→--ink-2(Δ43) · #c9c1b1→--border(Δ25) · #fffdf8→--surface-hi(Δ0). 발행된 글 21편의 시뮬 글자가 따뜻한 먹색에서 코발트 남색으로 바뀌는 것이 이 판정의 내용이고, 선례는 posts/tauri-2/PermissionGate.tsx:52-57 이었다. 착수 뒤 같은 역할의 변종 9건이 더 나왔고(#3c3a33 2 · #5b5748 5 · #5c5648 1 · #8a8272 1, Δ39~43) 같은 판정의 연장으로 처리했다 — 단은 명도가 아니라 역할로 갈랐다(§5 대전제). 배치2 가 이 배치로 미룬 --state-* 는 세우지 않기로 확정했다 — 시뮬의 const OK/DANGER/ACCENT/POP 가 --cat-planning/--cat-strategy/--cat-architecture/--pop 과 값이 정확히 같아 var() 치환만으로 닫힌다
- 2026-08-27T19:22 · s:4abb90c0 — 배치5 에서 드러난 게이트 인식층의 한계 둘 — 별도 카드감이고 KAN-072.7 범위다. ① React 인라인 스타일의 숫자값을 못 본다. JSXOBJ 추출이 prop: "문자열" 만 잡아 fontSize: 13 · borderRadius: 8 · gap: 12 · padding: 18 은 통째로 판정에 안 들어온다(S10 6파일에만 borderRadius 33곳 · fontSize 40곳). 값이 스케일과 같은 것은 옮겨도 화면 변화 0 이라 공짜지만 13.5px · borderRadius 10 같은 스케일 밖 값이 여기 숨어 있다. 정본 tauri-2/PermissionGate.tsx 도 숫자로 두고 있어 파일 단위로 고칠 것이 아니다. ② 삼항·템플릿 리터럴 안의 리터럴을 못 본다. 그래서 「기준선 건수」와 「파일 안 hex 개수」가 안 맞는다(S9 6파일: 게이트 171 vs hex 122 + px 101). 게이트 수만 보고 끝내면 hex 가 남고 hex 만 보면 게이트 수를 못 맞춘다 — 둘 다 재야 한다. 부작용 하나가 실측됐다: 판정 대기 리터럴을 const 로 끌어올리면 게이트 시야를 벗어나 「0」이 닫힌 것과 숨은 것을 섞는다(S10 에서 6건 발생, 되돌렸다)
- 2026-08-27T19:22 · s:4abb90c0 — 병렬 3 에서 새로 겪은 것 — 배치6 지시에 넣는다. ① 격리 사본을 /tmp 로 빼면 node_modules 심링크로는 빌드가 안 된다(vite 가 심링크 실제 경로를 사본 루트에 다시 붙여 /private/tmp/…/Users/…/node_modules 라는 없는 경로를 만든다). rsync -a --link-dest 로 하드링크하면 204MB 가 3.8초다. 사본이 레포 안일 땐 접두사를 공유해 우연히 되므로 밖으로 빼는 순간 터진다. 그리고 사본을 레포 안에 두면 tsc 가 중복으로 훑어 에러가 두 벌로 잡힌다(실측 4→2). ② rsync --exclude 'dist' 는 node_modules/astro/dist 도 지운다 — 앵커를 붙여 --exclude '/dist'. 세 세션 중 둘이 여기 걸렸다. ③ 빌드 HTML 을 그냥 diff 하면 46쪽 중 26쪽이 달라졌다고 나온다 — astro-island uid= 가 빌드마다 난수다. sed -E 's/ uid="[^"]*"//g' 로 지워야 진짜만 남는다. ④ 격리는 git checkout 대신 git show HEAD:<경로> 로 한다(공유 인덱스를 안 건드린다). ⑤ astro preview 는 --outDir 은 받지만 --port 를 무시하고 빈 포트를 잡으며 [::1] 에 바인딩한다 — 127.0.0.1 로는 안 붙고 localhost 로 붙여야 한다. ⑥ width:verify 의 --base 는 outDir 이 아니라 URL 이다. ⑦ dist-s9 같은 이름은 .gitignore 에 안 걸린다(dist 정확 일치)
- 2026-08-27T19:22 · s:4abb90c0 — 검증 하네스에서 조용히 통과하는 고장 셋 — 이것이 배치5 의 제일 값진 기록이다. ① 인라인 스타일 감사를 [style*='hex'] 로 세면 안 된다. React 는 클라이언트 재렌더 때 인라인 hex 를 rgb() 로 정규화하므로 조작이 건드린 노드 — 곧 이 배치가 꼭 봐야 하는 조건부 색 — 만 골라서 셀렉터에서 빠지고, 결과는 「0곳, 이상 없음」으로 나온다. 실측: S10 이 가짜 실패와 가짜 통과를 양쪽으로 겪었고 S11 에서 조작 후 hex 가 사라진 노드가 EP1 3 · EP3 3 · EP7 3 · EP6 1. getComputedStyle 로 세야 한다. ② 셀렉터에 리터럴을 박으면 치환 후 조용히 0 이 된다([style*='2px solid'] → var(--stroke-bold, 2px) solid). ③ 감사 스크립트의 0건 통과를 그냥 믿으면 안 된다 — 고장을 주입해 무는지 먼저 본다. 주입 방식이 갈린다: 토큰 이름을 지우면 이름 감사만 물고 화면은 안 바뀌며(fallback 이 정확히 같아서 — fallback 정확일치 규약이 값을 하는 지점이 여기다), 값만 지역에서 뒤집으면(--ink: #ff0000) 화면 대조만 문다. 두 축이 서로 다른 고장을 잡으므로 어느 쪽도 공회전이 아니다. 그 밖에 client:visible 아일랜드는 수화 전 클릭이 죽은 마크업에 떨어져 조용히 사라지고(대상 figure 의 조상 아일랜드 하나만 기다린다 — 전체 astro-island[ssr] 소멸을 기다리면 화면에 안 들어오는 섬 때문에 타임아웃난다), input[type=range] 는 Playwright fill() 이 안 먹으며(React 가 value setter 를 추적한다 — 포커스 후 ArrowLeft/Right), 클릭 직후 바로 읽으면 React 커밋보다 앞선다
