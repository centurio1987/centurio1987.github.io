# root - value 관계인 경우
value는 식별성과 연속성이 없다. 따라서, 도메인 모델일 때는 식별자를 갖지 않는다. 이를 RDB 스키마에서 가장 쉽게 표현하는 방법은, root를 플랫화 하는 것이다. 1:1 관계인 경우는 그 이상 고려할 부분이 없다. 하지만 1:n인 경우, 정규화 문제가 발생한다. 

## key 문제
도메인 모델에서는 value에 식별자가 필요하지 않다. 