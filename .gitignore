# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# astro
.astro

# Obsidian vault workspace
.obsidian/

# npm/pnpm lockfiles (CI uses bun)
package-lock.json
pnpm-lock.yaml

# stray skill backups (cannot be deleted in some sandboxes)
*.v1bak

# graphify 실행 부산물 — graph.json/labels/report 는 커밋하되 캐시와 날짜 백업은 제외
graphify-out/cache/
graphify-out/20*/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo
.vitepress/cache

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
