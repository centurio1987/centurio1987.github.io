---
card: KAN-075-7DF37H
batch: 3
created: 2026-08-29
branch: KAN-075-7DF37H
status: 계획
steps: S5, S6
---

# KAN-075-7DF37H 배치3 — 부채 29건 상환 (이 카드에 넣을지는 유저가 정한다)

카드: [KAN-075-7DF37H.md](../KANBAN.cards/KAN-075-7DF37H.md) · 범위 `S5` · `S6`
선행: [배치2](KAN-075-7DF37H.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 0. 이 배치는 조건부다

KAN-073 은 인식층만 열고 기준선을 778 로 세운 뒤 상환을 KAN-074 로 넘겼다. 같은 모양으로
가면 이 배치는 후속 카드가 되고, 이 카드는 배치2 에서 끝난다.

여기서는 상환이 **29건 · 6파일 · 고유 값 15개**라 한 배치에 들어간다. 그래서 두 길이 다 성립한다.

| | 이 카드에 넣는다 | 후속 카드로 뺀다 |
| --- | --- | --- |
| 이 카드 끝의 래칫 | **0** | 29 |
| 커밋 수 | 2 (인식층+기준선 29 / 상환+기준선 0) | 1 |
| `src/` 변경 | 있다 → `render:compare` 필요 | 없다 |
| 전례 | — | KAN-073 → KAN-074 |

## 1. 작업 패키지

### WP1 · `S5` 드리프트 21 + 위반 8 처분

| 파일 | 위반 | 드리프트 |
| --- | ---: | ---: |
| `src/styles/viz-frame.css` | 2 | 7 |
| `src/components/deco/Tape.astro` | 4 | 3 |
| `src/components/deco/patterns/PhotoFrame.astro` | 2 | 3 |
| `src/components/deco/PaperSurface.astro` | 0 | 4 |
| `src/components/deco/Polaroid.astro` | 0 | 3 |
| `src/components/deco/StickyNote.astro` | 0 | 1 |

- **드리프트 21건은 토큰 값을 rgba 로 전개해 다시 적은 것**이다 —
  `rgba(32, 38, 74, α)` 13건(`--ink` = `#20264A`) · `rgba(255, 255, 255, α)` 7건
  (`--tony-glint` = `#ffffff`) · `rgba(32,38,74,0.12)` 1건.
- **위반 8건은 테이프·크래프트 종이 계열 6색**(`rgba(240,222,180,.62)` ·
  `rgba(236,223,196,.6)` · `rgba(219,201,170,.95)` · `rgba(205,184,150,.95)` ·
  `rgba(255,252,246,.86)` · `rgba(255,252,246,.78)`). 토큰화하거나 **근거 문서의 위치를
  걸어** 예외로 올린다 — 위치 없는 예외는 게이트가 안 받는다.
- **`viz-frame.css` 와 `PhotoFrame.astro`·`Tape.astro` 는 같은 표현을 복제해 들고 있다**
  (감사 §3-9). 한쪽만 고치면 두 지면이 갈린다.

**완료 기준**: 29건이 0 이 되고 **`bun run render:compare` 가 차이 0** 을 낸다.
데코는 눈대중이 아니라 실측 좌표라(`deco-kit` 스킬) 눈으로 갈음하지 않는다.

### WP2 · `S6` 기준선 `29 → 0`

**완료 기준**: `bun run tokens:verify` exit 0, 자가검사 10종이 그대로 산다.

## 2. 의존과 순서

`S5` → `S6` 직렬. 배치2 전체가 선행이다.

## 3. 리스크

- **`rgba()` 전개를 `color-mix(in srgb, var(--ink) 7%, transparent)` 로 바꾸면 계산 경로가
  달라진다.** 값이 같아도 브라우저 반올림이 갈릴 수 있어 `render:compare` 가 정본이다.
- **데코 표면은 손그림 질감이라 미세한 색차가 보인다.** 위반 6색을 토큰으로 밀 때
  「조화롭게」가 아니라 근거로 정한다 — 판정할 수 없는 낱말 아래에서 만들어진 것이
  준수율 3.6% 구획이다.
- 되돌리기: `S5` 는 `src/` 를 고치므로 work 태그(`kan/KAN-075-7DF37H/S5`)로 revert 한다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다. -->
