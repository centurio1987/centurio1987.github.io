---
card: KAN-069-M724GH
batch: 2
created: 2026-08-24
branch: KAN-069-M724GH
status: 완료
steps: S4, S5, S6
---

# KAN-069-M724GH 배치2 — 판정하고, 순서를 매기고, 한 장으로 낸다

카드: [KAN-069-M724GH.md](../KANBAN.cards/KAN-069-M724GH.md) · 범위 `S4` · `S5` · `S6`
선행: [배치1](KAN-069-M724GH.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

### WP1 · `S4` 예외 판정

배치1 이 모은 리터럴을 **정당한 예외 / 드리프트 / 위반** 셋으로 가른다. 판정 규칙 하나:
**"정당한 예외"는 근거 문서의 위치를 걸 수 있을 때만 그렇다.** `DESIGN_CONCEPT.md` ·
`DECO_KIT.md` · `CLAUDE.md` · `deco-kit`/`graph-pipeline` 스킬 중 어디에도 근거가 없으면 위반으로
내린다 — 근거 없이 "일부러 그런 것 같다"로 넘기면 그게 곧 드리프트의 정착이다.

미리 아는 예외 후보(근거를 확인할 대상): `deco.css`(데코 키트는 선택 레이어) ·
`viz-frame.css`(27.9KB, viz 엔진 프레임) · `TalkLayout`(폭신 대담은 자기 시각 언어가 있고
`deco.css` 를 안 들여온다) · `src/pages/design/*`(카탈로그 지면).

**완료 기준**: 분류된 전 건 목록. 각 건에 (파일:줄, 값, 판정, 근거) 넷.

### WP2 · `S5` 개선 방안

축 A 미구현 층과 축 B 위반을 메우는 순서를 매긴다. 항목마다 넷을 단다 —
**무엇을 · 어디를 · 왜 지금 · 무엇으로 검증하는가.** 마지막 것이 빠지면 개선안이 아니라 소원이다.

후속 카드로 나갈 단위를 짚는다(이 카드는 리포트까지다). 재발 방지 게이트 제안도 여기 들어간다 —
이 레포는 이미 `viz:verify` · `deco:verify` · `check-emphasis` · `check-post-markers` 로 같은 일을
하고 있으므로, 형식은 그것들에 맞춘다.

**완료 기준**: 우선순위 매긴 개선 항목 목록. 넷이 다 있는 항목만 싣는다.

### WP3 · `S6` 리포트 산출

`design-concept/UI_CONSISTENCY_AUDIT.md` 로 정본을 쓰고 Artifact 로 발행한다.

**완료 기준**: 정본 파일 존재 + 카드 「검증」 절 5항 전부 통과 + Artifact URL 확보.

## 2. 의존과 순서

- 배치1 전체 → `S4` **하드 의존.** 원자료 없이 판정할 수 없다.
- `S4` → `S5` → `S6` 순차. 셋 다 앞의 산출물을 그대로 먹는다.
- 이 배치에 병렬 지점은 없다. 셋이 전부 종합 작업이고, 쪼개면 문서가 세 사람이 쓴 것처럼 갈린다.

## 3. 리스크

- **개선 방안이 목록만 되고 순서가 안 서는 것(S5).** "다 중요하다"는 우선순위가 아니다 →
  각 항목에 "왜 지금"을 강제해 순서를 만든다.
- **리포트가 길어져 안 읽히는 것(S6).** 전수 결과 전량을 본문에 쏟으면 결론이 묻힌다 →
  결론·판정표를 앞에 놓고 전 건 목록은 뒤 부록으로 내린다.
- **되돌리기**: `design-concept/` 에 파일 하나가 늘 뿐이다. `src/` 는 안 건드린다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 -->

**2026-08-24 착수. 단일 에이전트로 간다** — 2항이 이미 "이 배치에 병렬 지점은 없다"고 못 박았고
근거가 그대로 유효하다. `S4`·`S5`·`S6` 셋이 전부 앞의 산출물을 그대로 먹는 종합 작업이라,
쪼개면 리포트가 세 사람이 쓴 것처럼 갈린다.

**`S4`·`S5`·`S6` 셋 다 이 배치에서 닫는다 — 다음 배치로 미루지 않는다.** 배치1 이 원자료를
전부 뽑아 놨으므로(스크래치패드 12파일) 이 배치의 실제 무게는 판정과 집필이고, 새로 훑을
소스가 없다.

**배치1 산출물 인계 확인.** 스크래치패드가 세션마다 갈리므로 배치1 의 12파일을
`673b055a-…/scratchpad` 에서 이 세션 스크래치패드로 통째로 복사했다. `s3-scan.json`(681KB)이
`S4` 의 유일한 원자료다.

**워크트리**: `/Users/centurio/orca/workspaces/centurio1987.github.io/KAN-069-M724GH`
(브랜치 `KAN-069-M724GH`, 배치1 마지막 커밋 `fd93bc5`)

**2026-08-24 배치2 종료 — `S4`·`S5`·`S6` 3/3 닫힘. 카드의 work 6/6 이 끝났다.**

산출물은 정본 하나와 스크래치패드 판정기 넷이다.

| 산출물 | 어디 | 무엇 |
| --- | --- | --- |
| `design-concept/UI_CONSISTENCY_AUDIT.md` | **레포(정본)** | 진단 리포트 — 6절 + 부록 2 |
| Artifact | `https://claude.ai/code/artifact/85f8a1f2-49f6-4692-aaa7-8dec32fe0940` | 위 정본의 렌더(파생물) |
| `s4-classify.py` · `.json` | 스크래치패드 | 다섯 갈래 판정 + 보정 셋 |
| `s4-fallback.py` · `.json` | 스크래치패드 | `var(--x, fallback)` 108자리 대조 |
| `s4-docrules.py` · `.json` | 스크래치패드 | `DESIGN_CONCEPT.md` §9·§5 스케일 대조 |
| `s4-verdict.md` · `s5-plan.md` | 스크래치패드 | S4·S5 작업 산출 |

**검증 5종 전부 통과.** 정본 존재 · 판정기 넷 재실행 바이트 동일(총계 3,539 일치) · 모든 판정에
`파일:줄` 또는 인터페이스 필드명 · `bun run build` 46페이지 통과 + `src/` 변경 0건 ·
드리프트 값 쌍 3건 기재.

**계획에서 벗어난 것 둘 — 검토 판단 항목 후보다.**

1. **판정 갈래를 셋이 아니라 다섯으로 냈다.** 카드 `S4` 는 「정당한 예외 / 드리프트 / 위반」 셋을
   적었는데, 배치1 인계 3번대로 「토큰 미존재」를 갈랐고 축 누수용 「측정 제외」가 하나 더 붙었다.
   섞었으면 위반이 2,634건이라는 틀린 수가 나오고 고칠 수 있는 580건이 그 안에 묻힌다.
2. **`S5` 도중에 `S4` 를 보강했다.** `DESIGN_CONCEPT.md` §9(간격)·§5(글자 크기)가 이미 스케일을
   정해 뒀다는 것을 늦게 발견했다 — 「토큰 미존재」로 묶었던 1,977건 중 **987건이 실은 문서 규칙
   위반**이다. 위반 총계가 580 → **1,567**, 준수율이 56.7% → **44.3%** 로 바뀌었다.
   `S4` 는 이미 닫혀 있었으므로 보강분을 `s4-verdict.md` §10 과 `doc-log` 로 남기고 진행했다.

**검토로 넘길 판단 항목 넷.**

1. 원문의 `component`·`wrapper` 낱말 대응이 안 선다 — 읽기 3안(`s1-contract.md` §3).
   판정 자체는 안 흔들린다(어느 읽기든 검사 필드 집합이 같다).
2. **축 A 처분** — A(채운다) / **B(줄인다, 추천)** / C(그대로). 원문의 다섯 낱말을 지시로 읽으면
   A, 판정 기준선으로 읽으면 B다. 이 읽기를 정하는 것은 사람이다.
3. **선 굵기 스케일 값 선택**(P1-2) — `--stroke-hair: 1px` 를 더할 것인가, `1px` 로 통일할 것인가.
4. **판정기 넷을 레포에 넣지 않은 것.** 카드 전략의 결정("추출기는 스크래치패드에 두고 레포에는
   넣지 않는다")을 지켰지만, 그러면 **스크래치패드가 비는 순간 리포트 수치를 재현할 길이 없다.**
   개선안 P3-1(`tokens:verify`)이 그 판정 로직을 레포로 옮기는 항목이다 — 그 카드를 지금 열
   것인가, 아니면 판정기만 먼저 `scripts/` 에 넣을 것인가.
