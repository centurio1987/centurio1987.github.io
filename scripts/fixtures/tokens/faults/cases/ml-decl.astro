---
// 고장 — 줄바꿈을 넘는 선언 안의 토큰 밖 색.
// 옛 DECL 은 값 클래스가 [^;{}\n] 이라 이 선언을 통째로 못 본다.
//
// **`box-shadow` 로 쓰면 안 된다.** 실제 부채의 대표 사례는 여러 줄 `box-shadow` 지만
// (`src/components/Logo.astro:58`), 그림자 축에는 문서에 판정 가능한 규칙이 없어
// 「판정 불가」로 떨어진다 — 고장이 안 걸린다. 색 축 속성으로 심어야 게이트가 문다.
---
<style>
  .x {
    background: linear-gradient(
      180deg,
      #302d28,
      #1c1b18
    );
  }
</style>
