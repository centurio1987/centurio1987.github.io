---
// 고장 — 자간이 §5 의 4단 밖이다. 단위가 `px` 인 것 자체가 결함이다:
// Meta(13px)와 Label(12px)에 같은 절대값을 주면 실제로는 다른 값(0.038em · 0.042em)을
// 준 것이 된다. §5 는 자간 단위를 `em` 하나로 못박았다.
---
<style>.x { letter-spacing: 3px; }</style>
