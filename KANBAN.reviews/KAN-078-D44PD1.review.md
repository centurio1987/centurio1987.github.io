---
card: KAN-078-D44PD1
title: render:compare 가 대조를 안 하고도 「전후 동일」이라고 답한다 — 거짓 통과를 막는다
created: 2026-08-31
branch: KAN-078-D44PD1
worktree: /Users/centurio/orca/workspaces/centurio1987.github.io/KAN-078-D44PD1
base: f53fbed
status: 검토 대기
---

# KAN-078-D44PD1 검토 요청 — render:compare 가 대조를 안 하고도 「전후 동일」이라고 답한다 — 거짓 통과를 막는다

카드: [KAN-078-D44PD1.md](../KANBAN.cards/KAN-078-D44PD1.md)

> 이 문서는 **검토를 위한 산출물**이다. 수행 내역은 카드 실행 문서에 있고, 착수 전
> 계획은 배치 문서에 있다. 여기 있는 것은 "지금 이 브랜치를 무엇으로 판정하는가" 뿐이다.

## 1. 검토 대상

| 항목 | 값 |
|---|---|
| 브랜치 | `KAN-078-D44PD1` |
| 워크트리 | `/Users/centurio/orca/workspaces/centurio1987.github.io/KAN-078-D44PD1` |
| 베이스 | `f53fbed` |
| 변경 훑기 | `git diff f53fbed...HEAD` |

**커밋 7건**

```text
e890a7b kanban: KAN-078-D44PD1 S3 완료 — 실측으로 결말 확인, 배치1 닫음
fbf1f14 kanban: KAN-078-D44PD1 진행률 파생 줄 갱신 (work 2/3)
5b223c9 docs: 결말 규약을 파일 머리주석과 CLAUDE.md 에 적는다 (KAN-078 S3)
4c9aeb4 compare-render: 결말을 endOf()/finish() 하나로 모으고 거짓 통과 재현 자가검사를 붙인다 (KAN-078 S1·S2)
eaa95fc kanban: KAN-078-D44PD1 계획 리포트 발행
1d7fcc4 kanban: KAN-078-D44PD1 배치1 계획 — S1~S3 일직선, 병렬 폭 1
f42abc6 kanban: KAN-078-D44PD1 착수 — 카드 실행 문서 작성 + 진행 중 이동
```

**변경 파일 10개 (+874 −52)**

| 파일 | 상태 | 추가 | 삭제 |
|---|:--:|---:|---:|
| `.kanban/archive.jsonl` | M | 1 | 0 |
| `.kanban/log.md` | M | 1 | 1 |
| `.kanban/state.json` | M | 19 | 15 |
| `CLAUDE.md` | M | 8 | 1 |
| `KANBAN.batches/KAN-078-D44PD1.batch1.md` | M | 75 | 0 |
| `KANBAN.board.html` | M | 4 | 4 |
| `KANBAN.cards/KAN-078-D44PD1.md` | M | 84 | 0 |
| `KANBAN.md` | M | 12 | 10 |
| `KANBAN.reports/KAN-078-D44PD1.report.html` | M | 508 | 0 |
| `scripts/compare-render.ts` | M | 162 | 21 |

**롤백 태그 4개**

```text
kan/KAN-078-D44PD1/S1
kan/KAN-078-D44PD1/S2
kan/KAN-078-D44PD1/S3
kan/KAN-078-D44PD1/batch1
```

## 2. 검증 — 기준과 실행 결과

<!-- 기준은 카드 실행 문서 「검증」 절의 사본이다. 정본은 KANBAN.cards/KAN-078-D44PD1.md 이므로
     기준이 바뀌면 그쪽을 고치고 review-init --refresh 로 이 항만 다시 뜬다.
     결과는 착수한 쪽이 이미 돌린 것이다 — 검토자에게 다시 돌리라고 시키지 않는다.
     **다시 돌려 아래와 다르게 나오면 그 자체가 반려 사유다.** -->

**기준**

전부 이 카드의 워크트리에서 돈다. `bun install` 이 먼저다(새 체크아웃이라 `node_modules` 가 없다).

```bash
# 1) 결말 자가검사 — 빌드·브라우저 없이 매 실행 첫머리에 돈다
bun scripts/compare-render.ts        # src/ 에 변경이 없는 상태
#   → 「결말 자가검사 … 통과」가 찍히고, 마지막 줄에 「대조 안 함」이 있다. 종료코드 0

# 2) 이 카드의 본론 — 자가검사만 돌린 실행이 「전후 동일」로 안 끝난다
bun scripts/compare-render.ts --self-test 2>&1 | tail -3
#   → 마지막 줄에 「전후 동일」이 없고 「대조 안 함」이 있다. 고장 4종 + 잡음 0 은 종전대로

# 3) 옛 결말을 주입하면 무는가 — (1)·(2) 어느 쪽을 돌려도 결말 자가검사가 그 사례를 함께 판정한다
#    일부러 깨서 보려면 verdict() 표의 selftest 행을 「✓ 전후 동일.」로 되돌린다 → 첫머리에서 종료코드 3

# 4) 정상 대조 경로가 그대로인가 — src/ 를 한 줄 고쳐 놓고
bun scripts/compare-render.ts --pages /posts/tauri-2/
#   → 차이가 있으면 종료코드 1, 없으면 「✓ 전후 동일」 + 0

# 5) 회귀 없음
bun run build && bunx tsc --noEmit
```

**판정 기준은 마지막 줄과 종료코드 둘 다다.** 문구만 고치고 종료코드를 안 가르면 스크립트로
부르는 쪽에서 같은 거짓이 그대로 남는다.

**실행 결과**

```text
[1] 결말 자가검사 (빌드·브라우저 없이 매 실행 첫머리)
    bun scripts/compare-render.ts  →  「· 결말 자가검사 10건 통과 — 대조와 자가검사를 마지막 줄이 가른다」
    마지막 줄: 「· 변경 파일이 없다 (대조 안 함 — 잴 것이 없다).」 · 종료코드 0

[2] 이 카드의 본론 — 자가검사만 돌린 실행 (전체 실행, 사본 둘 + Chromium)
    bun scripts/compare-render.ts --self-test --ns kan078  →  종료코드 0
      ✓ 잡음 0 (같은 빌드끼리) — 차이 0
      ✓ 고장1 맨 var() 토큰 이름 삭제 — 80건
      ✓ 고장2 값 지역 반전 — 80건 (color rgb(32,38,74) → rgb(255,0,0))
      ✓ 고장3 속성 문자열 방식의 맹점 — [style*=] 0곳 · getComputedStyle 1770곳
      ✓ 고장4 색 함수 표기 정규화 — 같은 값은 같게, 다른 값은 다르게
    마지막 줄: 「✓ 자가검사 통과 (대조 안 함 — 전후를 재려면 옵션 없이 돌린다).」
    ← 옛 결말은 이 자리에서 「✓ 전후 동일.」이었다(KAN-077 S8 이 받은 그 줄)

[3] 고장 주입 3종 — 결말 자가검사가 무는가 (소스 사본에 주입, 각각 종료코드 3)
    ① 옛 결말(selftest 행을 PASS 로)        → 「selftest/0 문구 — "✓ 전후 동일."」
    ② finish() 밖 process.exit 신설          → 「종료 지점 — finish() 밖 process.exit — 803줄」
    ③ 갈래가 통과 문구를 직접 인쇄           → 「통과 문구 생산 지점 — PASS 밖에서 문구를 찍는다 — 852줄」
    셋의 사유가 서로 다르다(verify-talk 의 selfTestFaults 규약).

[4] 실제 대조 경로가 그대로인가
    src/styles/tokens.css 에 무해한 주석 한 줄 → bun scripts/compare-render.ts --pages /posts/tauri-2/
      · 계산된 스타일 차이 0 — 1쪽 × 뷰포트 2 × 상태 최대 9
    마지막 줄: 「✓ 전후 동일.」 · 종료코드 0   (주석은 실행 후 되돌렸다)

[5] 회귀
    bun run build → 46 page(s) built, Complete!
    bunx tsc --noEmit → scripts/compare-render.ts 에러 0 (레포 기존 에러 18건은 astro sync 미실행분으로 이 카드 전후 동일)
```

## 3. 판단 항목 — 스크립트가 판정할 수 없는 것

<!-- 스크립트가 판정할 수 없는 것만 적는다 — 값의 진위, 선택지 중 하나를 고른 근거,
     범위를 그은 자리. 2항에서 이미 돌아간 검증을 여기 옮겨 적지 않는다.
     한 줄 형식: 체크박스 하나에 의견 하나 — "<주제> — <지금 고른 값과 그 근거>".
     **의견마다 「상세」 접기가 따라붙는다** — 검토자는 이 카드를 수행하지 않았으므로
     내부 기호(`L10`·`P5`·`S8`)만 던지면 판정할 재료가 없다. 상세에는 그 기호를 풀어
     쓰고 원문 경로(`파일:줄`)나 링크를 건다.
     비어 있으면 "기계가 다 판정했고 사람이 정할 것이 없다"는 뜻이다. 그 판단도
     착수한 쪽이 하는 것이지 검토자가 빈칸을 보고 추측할 일이 아니다.
     **승계 절(3-0)이 있으면 그것이 먼저 온다** — 다른 검토서에서 넘어온 의견이고,
     판정은 승계를 받은 이 문서 하나에서만 내려진다. -->

**의견마다 판정과 추가 의견이 따로 붙습니다.** 판정은 상태이고 추가 의견은 말입니다 — 승인/반려를 아직
안 정했어도 의견 하나에만 추가 의견을 달 수 있고, 반대로 의견 하나만 먼저 닫을 수도 있습니다.
`<번호>`는 의견 순서이고, 주제의 문구 일부로도 찾습니다.

```
# 판정 — 승인 · 반려 · 철회
python3 scripts/kanban.py review-judge <project-root> --card KAN-078-D44PD1 --item <번호> --verdict 승인
# 추가 의견
python3 scripts/kanban.py review-note <project-root> --card KAN-078-D44PD1 --item <번호> --text "<추가 의견>"
# 추가 의견을 반영하다 새 의견이 생겼으면 (맨 뒤에 붙어 앞 번호가 안 밀립니다)
python3 scripts/kanban.py review-item <project-root> --card KAN-078-D44PD1 --add "<주제>
  <상세>"
```

**전체 승인은 살아있는 항목이 전부 승인일 때만 섭니다**(철회는 분모에서 빠집니다). 하나라도
반려·추가 의견·미정이면 4항의 전체 승인도 `→ 완료` 이동도 종료코드 14로 거부됩니다.

- [ ] -summary 의 종료코드를 0 에서 1 로 바꿨다 — 이대로 둘 것인가 (동작 변경 하나)
    - **상세** — 무엇이 문제였나: 요약 모드는 「무엇이 얼마나 움직였나」를 접어 보여 주는 자리인데, 차이를 수백 건 찾아 놓고도 종료코드 0 으로 끝났다. 화면을 눈으로 읽으면 알지만 스크립트로 감싸 부르면 통과로 읽힌다. 파일 머리주석은 원래부터 「차이가 있으면 종료코드 1」이라고 적고 있었다(scripts/compare-render.ts:37 부근, 옛 판의 같은 줄). 무엇을 했나: 차이가 있으면 1 로 낸다(scripts/compare-render.ts:102 의 summary 행). 대가: 이 모드를 성공 판정에 쓰던 자리가 있으면 이제 실패로 나온다 — 레포 안에는 없다(package.json:17 의 render:compare 는 옵션 없이 부른다). 정할 것: 이대로 둔다를 추천한다. 되돌리려면 그 한 행이다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] 결말 자가검사를 --self-test 뒤에 안 숨기고 매 실행 첫머리에 돌린다 — 동의하는가
    - **상세** — 무엇이 문제였나: 이 카드가 고친 고장이 「자가검사를 돌렸다」와 「전후를 대조했다」를 사람이 헷갈린 것이다. 그 구분을 지키는 검사를 다시 옵션 뒤에 두면 옵션을 안 붙인 날 조용히 통과한다. 무엇을 했나: 빌드도 브라우저도 안 타는 검사 10건을 main 첫 줄에서 돌린다(scripts/compare-render.ts:782 가 호출, 본체는 :141). 실측 체감 0초이고 출력은 한 줄 는다. 대가: 소스를 읽는 자기검사 둘이 붙어 이 파일에 표식 주석 규약이 생겼다(scripts/compare-render.ts:91 의 // pass-line, :66·:67·:121·:190 의 // exit-ok). 정할 것: 이대로 둔다를 추천한다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] 소스 자기검사가 표식 주석에 기댄다 — 이 수준에서 멈출 것인가
    - **상세** — 무엇이 문제였나: 「종료가 finish() 를 지나는가」 검사는 process.exit 이 있는 줄에 // exit-ok 주석이 붙어 있는지만 본다(scripts/compare-render.ts:178-181). 마음먹고 붙이면 통과한다. 왜 이 수준인가: 개수 래칫은 줄이 옮겨다닐 때마다 흔들리고, 제대로 하려면 이 파일 하나 때문에 TS 파서를 들여야 한다. 표식은 모르고 지나가는 것을 막지 알고 뚫는 것을 막지 못한다 — 그 한계를 알고 고르는 것이다. 정할 것: 여기서 멈춘다를 추천한다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] 「대조 안 함」이라는 낱말이 결말의 규약이 됐다 — 낱말을 이대로 둘 것인가
    - **상세** — 무엇이 문제였나: 대조를 안 한 결말은 마지막 줄에 반드시 이 다섯 글자를 담고, 자가검사가 그것을 확인한다(scripts/compare-render.ts:93 의 NOT_COMPARED 와 :148 의 speaksTruth). 그래서 문구를 다듬을 때 이 말은 못 뺀다. 대안은 값(compared)만 검사하고 문구는 자유롭게 두는 것인데, 그러면 사람이 실제로 읽는 자리에는 아무 강제가 없다. 이 카드가 고치는 고장이 정확히 「사람이 마지막 줄을 읽고 오해한 것」이라 문구 쪽에 걸었다. 정할 것: 낱말을 이대로 둔다를 추천한다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] CLAUDE.md 를 KAN-079 도 건드릴 텐데 지금은 겹침이 선언돼 있지 않다 — 그대로 둘 것인가
    - **상세** — 무엇이 문제였나: 이 카드의 scope 는 scripts/compare-render.ts 와 CLAUDE.md 이고(KANBAN.cards/KAN-078-D44PD1.md:5), 백로그의 KAN-079(토큰 게이트 하드월 승격)는 아직 카드 실행 문서가 없어 scope 미선언이다 — dep-check 의 unscoped_roots 13개 중 하나라 겹침 판정 자체가 안 돈다. 실제로 이 카드가 고친 것은 CLAUDE.md:194-202 의 render:compare 불릿 하나이고 KAN-079 가 고칠 것은 그 위의 토큰 게이트 래칫 문단(CLAUDE.md:170-186)이라 줄이 안 겹친다. 정할 것: 지금은 그대로 둔다를 추천한다 — KAN-079 착수 때 scope 를 선언하면 dep-check 가 그 시점에 다시 본다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._


## 4. 판정

<!-- 문서 하나에 대한 판정이다. **항목별로 갈리는 말은 여기 적지 않는다** — 3항 각 의견의
     「판정」과 「추가 의견」이 그 자리다. 여기 남는 것은 그 항목들이 전부 승인으로 닫혔다는
     사실 하나뿐이다.
     아래 「판정 이력」은 **덧붙기만 하는 이력**이다. 왕복이 돌면 줄이 쌓이고, 그것이 이 문서가
     무엇을 거쳐 승인에 닿았는지의 전부다 — 지우지 않는다. **판정에는 사유 칸이 없다** —
     승인은 대체로 덧붙일 말이 없고, 있다면 그것은 문서 전체가 아니라 그 항목에 대한
     말이라 3항의 「추가 의견」이 받는다.
     `review-judge --card KAN-078-D44PD1 --verdict 승인` 이 이 자리를 쓰고
     frontmatter 의 status 도 함께 고친다. 손으로 적어도 되지만, 그때는 수렴 검사를
     안 거치므로 `validate` 가 항목 판정과 어긋난 승인을 error 로 잡는다. -->

**판정**: (아직 없습니다)

**판정 이력**:

- 승인이면 → `apply --op move --id KAN-078-D44PD1 --to done` 뒤에 `main` 병합과 워크트리 정리(출력의 `cleanup`)
- 반려면 → `apply --op move --id KAN-078-D44PD1 --to doing` 뒤에 `doc-log --entry "<반려 사유>"`.
  요청서는 **지우지도 다시 뜨지도 않는다** — 고친 뒤 그 항목을 `review-judge --verdict 승인` 으로
  뒤집으면 같은 문서에서 수렴한다. 1·2항이 낡았으면 `review-init --refresh` 로 그 두 항만 간다.
