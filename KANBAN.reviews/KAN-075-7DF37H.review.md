---
card: KAN-075-7DF37H
title: 게이트 인식층 — 여러 줄에 걸쳐 쓴 CSS 선언 47건을 판정에 넣는다
created: 2026-08-29
branch: KAN-075-7DF37H
worktree: /Users/centurio/orca/workspaces/centurio1987.github.io/KAN-075-7DF37H
base: bad3b23
status: 검토 대기
---

# KAN-075-7DF37H 검토 요청 — 게이트 인식층 — 여러 줄에 걸쳐 쓴 CSS 선언 47건을 판정에 넣는다

카드: [KAN-075-7DF37H.md](../KANBAN.cards/KAN-075-7DF37H.md)

> 이 문서는 **검토를 위한 산출물**이다. 수행 내역은 카드 실행 문서에 있고, 착수 전
> 계획은 배치 문서에 있다. 여기 있는 것은 "지금 이 브랜치를 무엇으로 판정하는가" 뿐이다.

## 1. 검토 대상

| 항목 | 값 |
|---|---|
| 브랜치 | `KAN-075-7DF37H` |
| 워크트리 | `/Users/centurio/orca/workspaces/centurio1987.github.io/KAN-075-7DF37H` |
| 베이스 | `bad3b23` |
| 변경 훑기 | `git diff bad3b23...HEAD` |

**커밋 2건**

```text
3a6f9aa S5+S6: 부채 29건 처분 — 색 14 · 토큰 신설 4 · 흰색 환산 9 · 래칫 0 (KAN-075-7DF37H)
ca73d46 S1~S4: 여러 줄 CSS 선언을 판정에 넣는다 — 옛 히트 증분 0 · 새 기준선 29 (KAN-075-7DF37H)
```

**변경 파일 26개 (+591 −93)**

| 파일 | 상태 | 추가 | 삭제 |
|---|:--:|---:|---:|
| `.kanban/archive.jsonl` | M | 1 | 0 |
| `.kanban/log.md` | M | 1 | 1 |
| `.kanban/state.json` | M | 15 | 22 |
| `CLAUDE.md` | M | 15 | 4 |
| `KANBAN.batches/KAN-075-7DF37H.batch1.md` | M | 7 | 1 |
| `KANBAN.batches/KAN-075-7DF37H.batch3.md` | M | 23 | 1 |
| `KANBAN.board.html` | M | 4 | 4 |
| `KANBAN.cards/KAN-075-7DF37H.md` | M | 18 | 6 |
| `KANBAN.md` | M | 8 | 7 |
| `design-concept/UI_CONSISTENCY_AUDIT.md` | M | 21 | 0 |
| `package.json` | M | 1 | 0 |
| `scripts/check-recognize-invariant.ts` | M | 183 | 0 |
| `scripts/compare-render.ts` | M | 59 | 2 |
| `scripts/fixtures/tokens/faults/cases/ml-decl.astro` | M | 17 | 0 |
| `scripts/lib/tokens/extract.ts` | M | 15 | 2 |
| `scripts/lib/tokens/recognize/multilineDecl.ts` | M | 128 | 0 |
| `scripts/lib/tokens/recognize/types.ts` | M | 16 | 0 |
| `scripts/lib/tokens/types.ts` | M | 5 | 1 |
| `scripts/tokens-baseline.json` | M | 7 | 7 |
| `src/components/deco/PaperSurface.astro` | M | 4 | 4 |
| `src/components/deco/Polaroid.astro` | M | 3 | 3 |
| `src/components/deco/StickyNote.astro` | M | 1 | 1 |
| `src/components/deco/Tape.astro` | M | 7 | 7 |
| `src/components/deco/patterns/PhotoFrame.astro` | M | 5 | 5 |
| `src/styles/deco.css` | M | 18 | 6 |
| `src/styles/viz-frame.css` | M | 9 | 9 |

**롤백 태그 2개**

```text
kan/KAN-075-7DF37H/S4
kan/KAN-075-7DF37H/S6
```

## 2. 검증 — 기준과 실행 결과

<!-- 기준은 카드 실행 문서 「검증」 절의 사본이다. 정본은 KANBAN.cards/KAN-075-7DF37H.md 이므로
     기준이 바뀌면 그쪽을 고치고 review-init --refresh 로 이 항만 다시 뜬다.
     결과는 착수한 쪽이 이미 돌린 것이다 — 검토자에게 다시 돌리라고 시키지 않는다.
     **다시 돌려 아래와 다르게 나오면 그 자체가 반려 사유다.** -->

**기준**

<!-- 무엇을 실행해 무엇이 나오면 이 카드가 끝난 것인가. -->
1. **옛 히트 불변** — `bun scripts/verify-tokens.ts --json` 을 변경 전(`git show HEAD:` 격리 사본)과
   후로 각각 떠서 `src ≠ ml-decl` 히트를 `(src·axis·kind·prop·value·file·line·token·excluded)`
   키 집합으로 대조한다. **기대 증분 0.** KAN-073 은 옛 `src` 라벨을 단 히트가 1건 늘어
   화이트리스트를 뒀지만, 이 카드는 `DECL` 을 안 고치므로 화이트리스트가 없다.
2. **겹쳐 세지 않는다** — ① 새 매치 구간 안의 옛 `DECL` 매치 **0** ② 새 `src` 와 옛 `src` 의
   `(file, line, value)` 교집합 **0**. 1번은 옛 히트만 봐서 겹침을 못 잡는다.
3. **자가검사** — 맨몸 실행으로 「정상 1 + 고장 10종, 사유 10가지」. 유일성 키
   `(verdict, want, src)` 의 셋째가 `ml-decl` 이라 기존 아홉과 안 겹친다.
   **기준선 갱신보다 먼저 한다** — 죽은 게이트와 다 갚은 게이트가 화면에서 같은 모양이다.
4. **게이트가 실제로 문다** — 여러 줄 선언에 토큰 밖 색을 새로 심은 픽스처가 `ml-decl` 경로로
   위반 판정을 받는다. 「173건이 들어왔다」는 인식만 증명하지 판정을 증명하지 않는다.
5. **화면 변화 0** — 배치1·2 는 `scripts/` 와 문서만 고치므로
   `git status --porcelain src/ public/` 가 **비어 있음**으로 갈음한다.
   배치3 을 하면 `src/` 가 바뀌므로 그때는 **`bun run render:compare` 가 정본**이다(KAN-074).
6. **회귀 없음** — `bun run build` · `viz:verify` · `deco:verify` · `width:verify` · `talk:verify` ·
   `check-post-markers` · `check-emphasis`(+`--dist`) · `tsc --noEmit` 신규 0.
   `graph:verify` 는 `src/data`·`src/content` 를 안 건드리므로 대상 밖이다.
7. **CI 가 안 빨개진다** — 인식층 확장과 기준선 갱신이 **같은 커밋**에 들어간다.
   게이트는 `build` 잡에서 `astro build` 앞에 돌고 `deploy` 가 `needs: build` 로 물려 있다.

**실행 결과**

```text
[1] 옛 히트 불변 — bun run tokens:invariant -- --before <bad3b23 스냅샷> --self-test
    ① 옛 히트 불변 — 판정 4592건이 열 필드 키로 그대로다(옛 src css-decl · jsx-attr · style-obj · expr-literal · attr-css · style-num)
    ② 겹쳐 세지 않는다 — 새 판정 173건과 옛 히트의 (file,line,value) 교집합 0
    ③ 겹침 가드 둘 + 양성 대조 — 셋 다 산다
    ✓ 인식층 불변 검사 통과.
    · 고장 주입: 옛 DECL 값 클래스에 줄바꿈 허용(=이 카드가 피한 그 수정) → ① 「늘음 84」 · ② 「겹쳐 셈 105」로 빨개진다(exit 1)
    · 고장 주입: 가드 1 제거 → exit 1 · 가드 2 제거 → exit 1 (각각 따로 문다)

[2] 자가검사 — bun run tokens:verify
    · 자가검사: 정상 1 + 고장 10종, 사유 10가지   (KAN-073 시점 9종/9가지 → ml-decl 하나 추가)

[3] 게이트 — bun run tokens:verify  → exit 0
    히트 4246건 — 게이트 대상 4231 · 제외 15   (착수 전 4073 / 게이트 대상 4058)
    래칫 기준: 정당한 예외 8 · 준수 3639 · 판정 불가 1083   (드리프트 0 · 위반 0)
    기준선 갱신 순서: 자가검사 생존 확인 → --update-baseline (0 → 29 → 0)

[4] 화면 대조 — bun run render:compare  → exit 1 (의도한 차이)
    대조 34쪽(/ · /posts/ · /graph/ · /design/deco/ + 글 30편) × 뷰포트 2 × 상태
    차이 26줄, 전부 흰색 환산 하나(rgba(255,255,255,a) → rgba(255,253,248,a) 20줄 +
      rgba(255,252,246,a) → rgba(255,253,248,a) 6줄). 그 밖의 차이 0.
    → color-mix 치환 14자리와 신설 토큰 4개는 화면을 한 픽셀도 안 바꿨다.
    차이가 난 지면은 /graph/ 와 /design/deco/ 둘뿐이다 — 글 30편·홈·글 목록은 전부 0.
    compare-render --self-test: 자가검사 5종 — 통과 5 (잡음 0 · 고장1·2·3 · 고장4 색 함수 정규화)

[5] 회귀 — 전부 exit 0
    build · tokens:verify · viz:verify · deco:verify · width:verify · talk:verify ·
    tokens:invariant · check-post-markers · check-emphasis · check-emphasis --dist · check-quote-blocks
```

## 3. 판단 항목 — 스크립트가 판정할 수 없는 것

<!-- 스크립트가 판정할 수 없는 것만 적는다 — 값의 진위, 선택지 중 하나를 고른 근거,
     범위를 그은 자리. 2항에서 이미 돌아간 검증을 여기 옮겨 적지 않는다.
     한 줄 형식: 체크박스 하나에 의견 하나 — "<주제> — <지금 고른 값과 그 근거>".
     **의견마다 「상세」 접기가 따라붙는다** — 검토자는 이 카드를 수행하지 않았으므로
     내부 기호(`L10`·`P5`·`S8`)만 던지면 판정할 재료가 없다. 상세에는 그 기호를 풀어
     쓰고 원문 경로(`파일:줄`)나 링크를 건다.
     비어 있으면 "기계가 다 판정했고 사람이 정할 것이 없다"는 뜻이다. 그 판단도
     착수한 쪽이 하는 것이지 검토자가 빈칸을 보고 추측할 일이 아니다.
     **승계 절(3-0)이 있으면 그것이 먼저 온다** — 다른 검토서에서 넘어온 의견이고,
     판정은 승계를 받은 이 문서 하나에서만 내려진다. -->

**의견마다 판정과 추가 의견이 따로 붙습니다.** 판정은 상태이고 추가 의견은 말입니다 — 승인/반려를 아직
안 정했어도 의견 하나에만 추가 의견을 달 수 있고, 반대로 의견 하나만 먼저 닫을 수도 있습니다.
`<번호>`는 의견 순서이고, 주제의 문구 일부로도 찾습니다.

```
# 판정 — 승인 · 반려 · 철회
python3 scripts/kanban.py review-judge <project-root> --card KAN-075-7DF37H --item <번호> --verdict 승인
# 추가 의견
python3 scripts/kanban.py review-note <project-root> --card KAN-075-7DF37H --item <번호> --text "<추가 의견>"
# 추가 의견을 반영하다 새 의견이 생겼으면 (맨 뒤에 붙어 앞 번호가 안 밀립니다)
python3 scripts/kanban.py review-item <project-root> --card KAN-075-7DF37H --add "<주제>
  <상세>"
```

**전체 승인은 살아있는 항목이 전부 승인일 때만 섭니다**(철회는 분모에서 빠집니다). 하나라도
반려·추가 의견·미정이면 4항의 전체 승인도 `→ 완료` 이동도 종료코드 14로 거부됩니다.

- [ ] 흰색 9자리를 --surface-hi 로 옮겨 화면을 바꾼 것이 맞는가 — 지금은 옮겼고, 흰색이 파랑 7/255 만큼 따뜻해진다
    - **상세** — 무엇이 문제였나: 마스킹테이프의 광택 띠와 도트 테이프 바탕, 스크랩북 액자의 흰 삼각 홀더, 사진 액자 아래 반투명 띠 — 이 넷이 소스에 순백으로 적혀 있었다(rgba(255,255,255,a) 7자리 · rgba(255,252,246,a) 2자리). 게이트는 이것을 「토큰과 값이 같은데 리터럴로 다시 적었다」로 문다. 그런데 값이 같은 토큰은 마스코트 눈의 흰자 하나뿐이라(--tony-glint #ffffff) 그 이름을 쓰면 뜻이 안 맞는다.
      무엇을 했나: 종이 흰색의 코어 토큰 --surface-hi(#fffdf8)로 옮겼다. 근거 둘 — .claude/skills/deco-kit/SKILL.md:65 의 색 규약 「짝 없는 색만 --deco-* 로 신설하고 나머지는 코어 토큰으로 환산한다(시안 hex 를 통째로 쓰면 팔레트가 두 벌이 된다)」, 그리고 src/components/deco/Tape.astro:84-86 이 반투명 벨럼 테이프에 대해 이미 적어 둔 「순백은 쓰지 않는다 — 크림 종이 위의 흰 알파는 테이프가 아니라 색이 빠진 사각형으로 읽힌다」.
      대가: 화면이 실제로 바뀐다. #ffffff → #fffdf8 이라 파랑이 7/255 낮아지고 알파 0.5 에서 합성하면 3.5/255 다. render:compare 가 잡은 26자리가 전부 이것이고, 눈에 보이는 지면은 글 지도(/graph/)와 데코 카탈로그(/design/deco/) 둘뿐이다.
      정할 것: 이대로 둘 것인가, 아니면 --deco-gloss: #ffffff 를 새로 세워 화면 변화를 0 으로 되돌릴 것인가. 추천은 이대로다 — 새 이름을 세우면 흰색이 팔레트에 두 벌 서고, 그것이 감사가 지목한 「제2 팔레트」 그 모양이다(design-concept/UI_CONSISTENCY_AUDIT.md:216).

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] compare-render 를 이 카드가 두 군데 고친 것이 범위 안인가 — 지금은 이 카드에 넣었다
    - **상세** — 무엇이 문제였나 (가): 값을 안 바꾸고 표기만 토큰으로 옮겼는데 화면 대조기가 전건 차이로 답했다. 브라우저가 color-mix() 를 rgba() 가 아니라 color(srgb 0.12549 0.14902 0.290196 / 0.07) 로 직렬화하기 때문이다 — 255 를 곱하면 정확히 32·38·74 라 같은 색인데 글자가 다르다. color-mix 는 이 레포가 이미 쓰는 관용구라(src/components/deco/Tape.astro:78 · src/styles/viz-frame.css) 앞으로 알파 낀 값을 토큰으로 되돌릴 때마다 같은 잡음이 난다.
      무엇이 문제였나 (나): 대조할 지면을 글 주소(/posts/<slug>/)에서만 뽑고 있었다. 데코 부품이 실제로 앉는 홈·글 목록·글 지도·데코 카탈로그는 한 번도 안 봤다. 이번 변경의 차이 26줄이 전부 /graph/ 와 /design/deco/ 에서 나왔으므로, 안 넓혔으면 이 카드가 「화면 변화 0」으로 통과했을 것이다 — 대조기가 눈을 감은 자리에서 화면을 바꾼 셈이 된다.
      무엇을 했나: (가) 색 함수 표기 정규화를 스냅샷에 넣고(scripts/compare-render.ts:255-283), 그것이 진짜 색 차이를 삼키지 않는지 보는 고장4 를 자가검사에 함께 박았다(scripts/compare-render.ts:595-620 — 같은 값 color-mix 는 리터럴과 한 글자도 안 갈리고 다른 값은 갈린다). (나) 범위를 넓힐 때 지면 넷을 기계가 함께 넣게 했다(scripts/compare-render.ts:111,127) — 손으로 --pages 를 붙이는 규율로 두면 안 붙인 날 조용히 통과한다.
      대가: 정규화가 1/255 보다 작은 색 차이를 삼킨다. 그보다 큰 차이는 그대로 나오고 고장4 가 매번 확인한다. 그리고 이 카드가 손댄 자리가 게이트(scripts/lib/tokens/)를 넘어 검증 도구까지 갔다.
      정할 것: 이 둘을 이 카드에 두는 것이 맞는가, 아니면 별도 카드로 빼고 이 카드는 --pages 를 손으로 붙인 결과만 남길 것인가. 추천은 이 카드에 두는 것이다 — 없으면 이 카드의 「화면이 안 바뀌었다」를 증명할 수 없고, 빼 두면 그 사이 다음 데코 카드가 같은 눈먼 자리에서 통과한다.

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._


## 4. 판정

<!-- 문서 하나에 대한 판정이다. **항목별로 갈리는 말은 여기 적지 않는다** — 3항 각 의견의
     「판정」과 「추가 의견」이 그 자리다. 여기 남는 것은 그 항목들이 전부 승인으로 닫혔다는
     사실 하나뿐이다.
     아래 「판정 이력」은 **덧붙기만 하는 이력**이다. 왕복이 돌면 줄이 쌓이고, 그것이 이 문서가
     무엇을 거쳐 승인에 닿았는지의 전부다 — 지우지 않는다. **판정에는 사유 칸이 없다** —
     승인은 대체로 덧붙일 말이 없고, 있다면 그것은 문서 전체가 아니라 그 항목에 대한
     말이라 3항의 「추가 의견」이 받는다.
     `review-judge --card KAN-075-7DF37H --verdict 승인` 이 이 자리를 쓰고
     frontmatter 의 status 도 함께 고친다. 손으로 적어도 되지만, 그때는 수렴 검사를
     안 거치므로 `validate` 가 항목 판정과 어긋난 승인을 error 로 잡는다. -->

**판정**: (아직 없습니다)

**판정 이력**:

- 승인이면 → `apply --op move --id KAN-075-7DF37H --to done` 뒤에 `main` 병합과 워크트리 정리(출력의 `cleanup`)
- 반려면 → `apply --op move --id KAN-075-7DF37H --to doing` 뒤에 `doc-log --entry "<반려 사유>"`.
  요청서는 **지우지도 다시 뜨지도 않는다** — 고친 뒤 그 항목을 `review-judge --verdict 승인` 으로
  뒤집으면 같은 문서에서 수렴한다. 1·2항이 낡았으면 `review-init --refresh` 로 그 두 항만 간다.
