---
card: KAN-071-6361MY
title: 선언 층을 줄인다 — bbangtoTonyStyleGuide export 를 걷고 foundation 만 남긴다
created: 2026-08-28
branch: KAN-071-6361MY
worktree: /Users/centurio/orca/workspaces/centurio1987.github.io/KAN-071-6361MY
base: 49f3e43
status: 검토 대기
---

# KAN-071-6361MY 검토 요청 — 선언 층을 줄인다 — bbangtoTonyStyleGuide export 를 걷고 foundation 만 남긴다

카드: [KAN-071-6361MY.md](../KANBAN.cards/KAN-071-6361MY.md)

> 이 문서는 **검토를 위한 산출물**이다. 수행 내역은 카드 실행 문서에 있고, 착수 전
> 계획은 배치 문서에 있다. 여기 있는 것은 "지금 이 브랜치를 무엇으로 판정하는가" 뿐이다.

## 1. 검토 대상

| 항목 | 값 |
|---|---|
| 브랜치 | `KAN-071-6361MY` |
| 워크트리 | `/Users/centurio/orca/workspaces/centurio1987.github.io/KAN-071-6361MY` |
| 베이스 | `49f3e43` |
| 변경 훑기 | `git diff 49f3e43...HEAD` |

**커밋 4건**

```text
8ab8b36 KAN-071-6361MY S3: CI 주석이 없어진 심볼을 안 가리키게 한다 — 소비 심볼과 소스 파일을 갈라 적는다
1884229 KAN-071-6361MY S2: 어긋난 값 셋을 tokens.css 에 맞춘다 — motion 3자리 · viz 2자리
9fc6ea7 KAN-071-6361MY S1: StyleGuide export 를 걷는다 — 소비처 0건이던 선언과 Phase C 예고 제거
cd76c12 kanban: KAN-071-6361MY 진행 중으로 이동 — 선언 층 처분 착수
```

**변경 파일 9개 (+62 −52)**

| 파일 | 상태 | 추가 | 삭제 |
|---|:--:|---:|---:|
| `.github/workflows/main.yml` | M | 3 | 1 |
| `.kanban/archive.jsonl` | M | 1 | 0 |
| `.kanban/log.md` | M | 1 | 1 |
| `.kanban/state.json` | M | 15 | 15 |
| `KANBAN.board.html` | M | 4 | 4 |
| `KANBAN.cards/KAN-071-6361MY.md` | M | 10 | 3 |
| `KANBAN.md` | M | 11 | 11 |
| `src/lib/motion/bbangtoTonyStyleGuide.ts` | M | 15 | 15 |
| `src/lib/viz/blogVizStyleGuide.ts` | M | 2 | 2 |

**롤백 태그 3개**

```text
kan/KAN-071-6361MY/S1
kan/KAN-071-6361MY/S2
kan/KAN-071-6361MY/S3
```

## 2. 검증 — 기준과 실행 결과

<!-- 기준은 카드 실행 문서 「검증」 절의 사본이다. 정본은 KANBAN.cards/KAN-071-6361MY.md 이므로
     기준이 바뀌면 그쪽을 고치고 review-init --refresh 로 이 항만 다시 뜬다.
     결과는 착수한 쪽이 이미 돌린 것이다 — 검토자에게 다시 돌리라고 시키지 않는다.
     **다시 돌려 아래와 다르게 나오면 그 자체가 반려 사유다.** -->

**기준**

**순서가 규약이다. `build` 가 맨 앞이다** — `verify-viz.ts:123-125` 는 `dist/` 가 없으면
`"verify-viz: dist/ 가 없다. bun run build 를 먼저 돌려라."` 를 내고 `exit 2` 로 죽는다. 새 orca
워크트리에는 `dist/` 가 없으므로 순서를 바꾸면 무조건 실패하고, 남아 있더라도 그건 이번 변경이
안 들어간 옛 빌드다.

1. **빌드가 선다** — `bun run build` 통과(최근 실측 46쪽 — 고정값으로 박지 말고 실측을 남긴다).
2. **렌더 결과를 실측한다** — `diff -r <스크래치>/dist-before dist`(S0 사본과 대조).
   `blogVizStyleGuide` 변경의 **유일한 그물**이다. 차이가 나면 어느 글의 어느 그림인지 지목해
   수행 내역에 적는다. `git diff` 로는 안 보인다 — `dist` 는 `.gitignore:6` 로 무시되고, 로컬 viz
   kind 셋은 폰트를 값이 아니라 CSS 변수 참조로만 들고 있어(`ProcessSteps.tsx:134,146` ·
   `PosterHero.tsx:72-73` · `Comparison.tsx:128,151,207` 의 `vvar("typography","titleFont")`)
   소스도 안 바뀐다.
3. **viz 게이트** — `bun run viz:verify` 통과(최근 실측 43개). 폰트 메트릭이 곧 판정이므로 S0 과 대조.
4. **생성물이 안 흔들렸다** — `bun run gen:motion && git diff --exit-code -- src/styles/motion.css`.
   S0 에서 이미 diff 가 있었으면 패키지 버전 탓이고 S2 와 무관하다.
5. **토큰 게이트가 안 늘었다** — `bun run tokens:verify` 가 비0 으로 안 끝난다. 래칫은 "늘면 실패 /
   같으면 통과 / **줄면 통과 + 갱신 안내**"라(`verify-tokens.ts:18-19`) 기대값은 「기준선과 같다」가
   아니라 **「늘지 않는다」**다. 두 TS 파일은 확장자 때문에 스캔 대상이 아니므로(`extract.ts:74`)
   실제로는 카운트가 안 움직이는 것이 정상이다.
6. **심볼이 사라졌다** — `rg -n '\bbbangtoTonyStyleGuide\b' src scripts .github -g '!scripts/fixtures/**'`
   가 파일명 언급만 낸다. `bunx tsc --noEmit` 이 S0 의 기존 2건 외 새 에러 0건.
7. **회귀 없음** — `bun scripts/check-post-markers.ts` · `bun scripts/check-emphasis.ts`(+`--dist`) ·
   `bun scripts/check-quote-blocks.ts` · `bun run deco:verify` · `bun run width:verify` ·
   `bun run talk:verify` 전부 통과. **`bun run graph:verify` 는 착수 전부터 실패 중이다**(실행 계획
   S0 참고) — `src/data/`·`src/content/` 변경이 0건이면 이 카드와 무관하므로 카드 탓으로 읽지 않는다.

**`public/images/**/hero.webp`(27개)는 다시 굽지 않는다** — 재래스터는 이 카드 범위 밖이다.
글리프 단위 폴백 변화가 래스터에 미치는 영향은 검토 판단 항목으로 사람에게 올린다.

**실행 결과**

```text
검증은 착수한 쪽이 이미 돌렸다. 순서는 build 가 맨 앞 — verify-viz.ts:123-125 가 dist/ 없으면 exit 2 로 죽는다.

[S0 기준선 — 아무것도 고치기 전, 같은 워크트리에서]
  build 46쪽 · dist 사본 200파일
  gen:motion → motion.css drift 0 (core@1.1.1 · tokens@1.2.0 · catalog@0.3.0, 10036 bytes)
  viz:verify  글 27편 / figure 43개, 텍스트 넘침 0
  tokens:verify 통과 (준수 2703 · 판정 불가 891)
  tsc 에러 2건 (둘 다 scripts/fixtures/tokens/reference/s2-declared-layers.ts:1,2 가 지워진
                KAN-069 워크트리를 절대경로 import — 이 카드는 안 고친다)
  graph:verify 실패 (글 4편 미등재: algorithm-at-40-prologue · aside-arc · orca-dev-env · welcome)

[변경 후 — 전항]
  1. build            46쪽 통과
  2. diff -r dist-before dist   16개 페이지 변경. 내용은 정확히 의도한 것뿐이다 —
                      BEFORE 에만 system-ui 166건 · JetBrains Mono 166건, AFTER 는 각 0건.
                      그 외 변경 0. (이것이 blogVizStyleGuide 변경의 유일한 그물이다 —
                      extract.ts:74 가 .astro|.tsx|.css 만 담아 .ts 는 토큰 게이트가 안 본다)
  3. viz:verify       글 27편 / figure 43개, 넘침 0 — S0 과 동일.
                      폰트 메트릭이 곧 판정인 게이트라 이 항이 폴백 축소의 실측이다.
  4. gen:motion       motion.css drift 0 — S0 과 동일 (생성기가 --bbangto-motion-* 만 추린다)
  5. tokens:verify    통과, 래칫 안 늘어남
  6. 심볼 참조        rg -n '\bbbangtoTonyStyleGuide\b' src scripts .github
                      -g '!scripts/fixtures/**' → 파일명 언급만 남고 코드 참조 0건
     tsc              에러 2건 — S0 과 동일, 새 에러 0
  7. 회귀             check-post-markers · check-emphasis · check-emphasis --dist ·
                      check-quote-blocks · deco:verify · width:verify · talk:verify 전부 ok
     graph:verify     실패하지만 S0 과 글자 그대로 같고 src/data·src/content 변경 0건이라 무관

다시 돌려 위와 다르게 나오면 그 자체가 반려 사유다.
```

## 3. 판단 항목 — 스크립트가 판정할 수 없는 것

<!-- 스크립트가 판정할 수 없는 것만 적는다 — 값의 진위, 선택지 중 하나를 고른 근거,
     범위를 그은 자리. 2항에서 이미 돌아간 검증을 여기 옮겨 적지 않는다.
     한 줄 형식: 체크박스 하나에 의견 하나 — "<주제> — <지금 고른 값과 그 근거>".
     **의견마다 「상세」 접기가 따라붙는다** — 검토자는 이 카드를 수행하지 않았으므로
     내부 기호(`L10`·`P5`·`S8`)만 던지면 판정할 재료가 없다. 상세에는 그 기호를 풀어
     쓰고 원문 경로(`파일:줄`)나 링크를 건다.
     비어 있으면 "기계가 다 판정했고 사람이 정할 것이 없다"는 뜻이다. 그 판단도
     착수한 쪽이 하는 것이지 검토자가 빈칸을 보고 추측할 일이 아니다.
     **승계 절(3-0)이 있으면 그것이 먼저 온다** — 다른 검토서에서 넘어온 의견이고,
     판정은 승계를 받은 이 문서 하나에서만 내려진다. -->

**의견마다 판정과 추가 의견이 따로 붙습니다.** 판정은 상태이고 추가 의견은 말입니다 — 승인/반려를 아직
안 정했어도 의견 하나에만 추가 의견을 달 수 있고, 반대로 의견 하나만 먼저 닫을 수도 있습니다.
`<번호>`는 의견 순서이고, 주제의 문구 일부로도 찾습니다.

```
# 판정 — 승인 · 반려 · 철회
python3 scripts/kanban.py review-judge <project-root> --card KAN-071-6361MY --item <번호> --verdict 승인
# 추가 의견
python3 scripts/kanban.py review-note <project-root> --card KAN-071-6361MY --item <번호> --text "<추가 의견>"
# 추가 의견을 반영하다 새 의견이 생겼으면 (맨 뒤에 붙어 앞 번호가 안 밀립니다)
python3 scripts/kanban.py review-item <project-root> --card KAN-071-6361MY --add "<주제>
  <상세>"
```

**전체 승인은 살아있는 항목이 전부 승인일 때만 섭니다**(철회는 분모에서 빠집니다). 하나라도
반려·추가 의견·미정이면 4항의 전체 승인도 `→ 완료` 이동도 종료코드 14로 거부됩니다.

- [ ] viz 의 폰트 폴백을 줄인 것이 맞는가 — system-ui 와 'JetBrains Mono' 를 뺐고, 실측상 지금 화면은 안 바뀐다
    - **상세** — 폴백은 실행 단위가 아니라 글리프 단위다. Gowun Dodum 이 못 가진 글자(한자·일부 기호·라틴 확장)는 이제 system-ui 대신 generic sans-serif 로, Space Mono 가 못 가진 글자는 'JetBrains Mono' 대신 플랫폼 기본 monospace 로 간다. 하필 그 JetBrains Mono 를 scripts/render-viz.ts:50-51 의 FONT_IMPORT 가 명시적으로 로드하고 있어서, 지금까지 웹폰트로 해결되던 폴백 하나가 플랫폼 폰트로 내려간다(메트릭이 다르다). 실측으로는 bun run viz:verify 가 figure 43개 전부 넘침 0 을 냈고 이는 착수 직후 아무것도 고치기 전에 같은 워크트리에서 뜬 기준선(카드 실행 문서 「실행 계획」 절의 첫 항목)과 같은 수다 — 이 게이트는 scripts/verify-viz.ts:36 이 SVG text 바운딩박스를 허용오차 1.5 로 재므로 폰트 메트릭이 곧 판정이다. 변경 전 dist 사본과의 diff 도 system-ui·JetBrains Mono 문자열 제거(각 166건 → 0건) 외에는 0 이다. 정본은 src/styles/tokens.css:203-204 이고 TS 값이 사본이었다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] viz 값을 tokens.css 에 맞춘 것 자체가 맞는가 — blogVizStyleGuide 는 안 쓰이는 선언이 아니라 실제 주입되는 쪽이다
    - **상세** — 감사 P4(design-concept/UI_CONSISTENCY_AUDIT.md:474)가 승인한 B안의 문면은 bbangtoTonyStyleGuide 의 StyleGuide export 를 걷는 것까지다. 그런데 어긋난 값 셋 중 폰트 2건이 src/lib/viz/blogVizStyleGuide.ts:79-80 에 글자 그대로 복제돼 있고 그쪽은 src/components/viz/VizFigure.tsx:128 과 scripts/render-viz.ts:70-71 이 실제로 주입한다 — 감사가 미래형으로 쓴 '주입되는 순간 갈라진다'가 viz 지면에서는 현재형이었다. 그래서 유저 결정(2026-08-28)으로 두 파일을 함께 정렬했다. 카드 scope 에 이미 blogVizStyleGuide.ts 와 render-viz.ts 가 들어 있었다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] scope 를 .github/workflows/main.yml 로 넓힌 것 — 원래 KAN-070 소유 파일이고 고친 것은 주석 세 줄이다
    - **상세** — .github/workflows/main.yml:60 이 (bbangtoTonyStyleGuide) 를 확장자 없이 괄호로 들고 있어 심볼명처럼 읽혔는데, CI 가 실제로 소비하는 것은 bbangtoTonyFoundation 이다. export 를 걷으면 없는 심볼을 가리키게 되므로 파일과 심볼을 갈라 적었다(실행부 :63-66 은 안 건드렸다). KAN-070 은 완료 카드라 dep-check 의 루트 독립성 판정 대상이 아니고, 실측상 미완료 루트 14개 중 scope 가 있는 카드가 이것 하나뿐이라 판정에 영향이 없다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._

- [ ] 파일명을 안 바꾼 것 — bbangtoTonyStyleGuide.ts 안에 이제 그 이름의 export 가 없다
    - **상세** — 파일명은 카드 메모가 유지로 못 박았다. scripts/gen-motion-css.ts:23 이 모듈 지정자 문자열로, .github/workflows/main.yml 이 주석으로 들고 있기 때문이다. 대신 파일 헤더에 '이 export 는 KAN-071 이 걷었다 — 다시 만들지 마라' 와 근거 위치를 남겼고, main.yml 주석도 그 사실을 적는다. 이름과 내용이 어긋난 채로 남는 것을 감수한 판단이다

    > **판정** — _아직 없습니다._

    > **추가 의견** — _아직 없습니다._


## 4. 판정

<!-- 문서 하나에 대한 판정이다. **항목별로 갈리는 말은 여기 적지 않는다** — 3항 각 의견의
     「판정」과 「추가 의견」이 그 자리다. 여기 남는 것은 그 항목들이 전부 승인으로 닫혔다는
     사실 하나뿐이다.
     아래 「판정 이력」은 **덧붙기만 하는 이력**이다. 왕복이 돌면 줄이 쌓이고, 그것이 이 문서가
     무엇을 거쳐 승인에 닿았는지의 전부다 — 지우지 않는다. **판정에는 사유 칸이 없다** —
     승인은 대체로 덧붙일 말이 없고, 있다면 그것은 문서 전체가 아니라 그 항목에 대한
     말이라 3항의 「추가 의견」이 받는다.
     `review-judge --card KAN-071-6361MY --verdict 승인` 이 이 자리를 쓰고
     frontmatter 의 status 도 함께 고친다. 손으로 적어도 되지만, 그때는 수렴 검사를
     안 거치므로 `validate` 가 항목 판정과 어긋난 승인을 error 로 잡는다. -->

**판정**: (아직 없습니다)

**판정 이력**:

- 승인이면 → `apply --op move --id KAN-071-6361MY --to done` 뒤에 `main` 병합과 워크트리 정리(출력의 `cleanup`)
- 반려면 → `apply --op move --id KAN-071-6361MY --to doing` 뒤에 `doc-log --entry "<반려 사유>"`.
  요청서는 **지우지도 다시 뜨지도 않는다** — 고친 뒤 그 항목을 `review-judge --verdict 승인` 으로
  뒤집으면 같은 문서에서 수렴한다. 1·2항이 낡았으면 `review-init --refresh` 로 그 두 항만 간다.
