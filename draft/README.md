# draft/

`raws/`의 메모를 토대로 스킬이 생성하는 **초안 골격**과 작성 중인 글이 머무는 공간.

- `post-draft` 스킬이 `<원본>-draft.md`를 여기에 만든다 (플롯 후보 + 토픽 특화 주의사항).
- 사용자가 플롯을 고르고 본문을 채운다.
- 완성되면 `publish-post` 스킬이 `src/content/posts/`로 옮기고 이 파일을 정리한다.

발행 전 단계의 작업물만 둔다 — 정식 글은 `src/content/posts/`에 있다.
